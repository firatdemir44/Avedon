# Açık İşler / Yapılacaklar

Bu dosya, sohbet geçmişinde kaybolmaması gereken önemli notları ve bekleyen kararları tutar. Yeni bir çalışma oturumuna başlarken önce burası kontrol edilir.

## GÜNCEL AÇIK LİSTE (2026-09-18 toparlaması; oturuma başlarken ÖNCE burası)

Aşağıdaki "Açık İşler" bölümü tarihsel kayıttır; güncel durum bu listedir. Bir iş kapanınca buradan silinir.

**A. Fırat'ta bekleyenler**
1. **SMS başvurusu (İleti Merkezi):** kod + `/api/health` `sms` teşhisi hazır. Başlık onayı şirket evrakı + KEP + e-Devlet BTK e-Kayıt ister (1-2 iş günü). Adımlar ve destek sorusu: `docs/sms-saglayici.md`. Hangi şirket adına başvurulacağı Fırat kararı.
2. **Gerçek WhatsApp numarası / SIM:** şu an Meta test numarası (en çok 5 alıcı).
3. **Telefonda kontrol:** Faz 2 (bildirim/izleme, teklif, Asistana sor, makine parkı, referanslar, iplik dizini) ve Faz 3 Adım 1-2 (çoklu teklif, karşılaştırma, asistanla teklif toplama). Adres: https://avedon-blond.vercel.app
4. **Melide'nin ilk 3 ürünü:** kompozisyon satırları için bir kez açılıp kaydedilmeli.
5. **Hesaplayıcılarda "görsel grafik" istekleri:** Fırat iletecek (2026-09-15'ten beri bekliyor).

**B. SMS'e bağlı kod işleri**
6. Faz 2 Adım 4: davet mekaniği ("tedarikçini davet et").
7. İlk dış pilot firma öncesi son kontrol: `WHATSAPP_APP_SECRET` canlıda dolu, SMS health yeşil.

**C. Faz 3:** 7 adımın hepsi kodlandı (2026-09-21). Sipariş geçmişi, güven özeti ve fiyat endeksi gerçek kullanıcı verisiyle dolacak; SMS girişi açılmadan canlıda boş görünürler (beklenen).

**D. Bilinen küçük eksikler (kod)**
8. Push bildirimi yok (uygulama 60 sn'de bir yokluyor).
10. Video yalnızca akış gönderilerinde; kararlaştırılan kapsamın kalanı (ürün sayfası videosu, sohbette video) yapılmadı.
11. WhatsApp'tan ürün taslağı (fotoğraf gönder → pasaport taslağı) yok. (Etiketten iplik okuma 2026-09-21'de eklendi: `/api/yarns/extract` + iplik formunda "Etiketten doldur"; yapılandırılmış çıktıda en çok 16 nullable alan sınırı yüzünden şemada boş değerler "" / 0 ile gösteriliyor.)
12. (Kapandı 2026-09-21: asistanın `teklif_topla` aracı iplikte de çalışıyor.) Aday kartındaki işaretler sohbetten çıkıp dönünce sıfırlanıyor. Asistanda `benzer_kumas_ara` (ürün koduyla) eklendi (2026-09-21); sonuç şimdilik genel özet kartıyla görünüyor, özel kart yok.
13. Teknik borç: kumaş ve iplik formlarında karışım/sertifika satır düzenleyicileri kopya (ortak bileşene çıkarılacak); `Product.imageUrl` eski sütunu silinecek; `mobile/MOBILE-DESIGN.md`'de Faz 2-3 ekran kalıpları yazılmadı.
14. Evden telefonda GELİŞTİRME sürümü testi çözülmedi (canlı web adresi yettiği için ihtiyaç yok).

## Açık İşler

- **SMS (Faz 1 Adım 8) EN SONA BIRAKILDI (Fırat kararı, 2026-09-17).** Karşılaştırma ve öneri hazır: `docs/sms-saglayici.md` (öneri İleti Merkezi). SMS bağlanana kadar gerçek kullanıcılar kendi başına giriş yapamaz (kod yalnızca sunucu kayıtlarında); dışarıdan ilk pilot firma alınmadan önceki son iş bu olmalı.

- **FAZ 1 ADIM 9 MOBİL KODLANDI, TARAYICI/TELEFON KONTROLÜ BEKLİYOR (2026-09-16):** Asistanın adı ve yüzü geldi. Yeni paket **`react-native-svg` 15.15.4** (`npx.cmd expo install` ile, SDK 57 uyumlu — `npm.cmd install` gerektirir). Yeni dosyalar: `mobile/src/components/AssistantAvatar.tsx` (İpek/Mert çizim büst, `size` + `state`: idle/listening/thinking/speaking/result), `mobile/src/components/PersonaPicker.tsx` (iki seçim kartı + sunucuya ulaşılamazsa `FALLBACK_PERSONA_OPTIONS`), `mobile/src/features/useReduceMotion.ts` (Skeleton'daki yerel kopya buraya taşındı). `AssistantScreen`: kişilik seçilmemişse (`persona: null`) sohbet yerine "Kim yardımcı olsun?" görünümü; sohbet boşken 112px avatar + sunucudan gelen karşılama (sohbete YAZILMAZ); balon avatarı 38px çizim oldu; üst bant alt satırı artık "Firma adı · İpek" (beceri sayısı karşılama ipucuna taşındı). `AssistantMemoryScreen`: en üstte "Asistan" bölümü + "Değiştir". **Doğrulanan:** `tsc` temiz, web paketi derleniyor. **Denenmedi:** avatarın gerçek görünümü (tarayıcı/telefon), animasyon akıcılığı, karakter seçimi ve değiştirmenin uçtan uca akışı. **Bilinen eksik:** avatar yalnızca çizim (ses/video yok, karar gereği); karşılama saat/bekleyen iş bilgisiyle ekran açılışında bir kez çekiliyor, sohbet içinde yenilenmiyor.

- **FAZ 1 ADIM 6 MOBİL KODLANDI, TELEFONDA KONTROL BEKLİYOR (2026-09-16):** Akıştaki ürünlü gönderi artık **kumaş pasaportu kartı** (`mobile/src/components/PassportCard.tsx`: kod + "KUMAŞ PASAPORTU", gramaj/en/örgü sütunları, kompozisyon + stok/MOQ/termin, sertifika rozetleri; eski ölçü şeridi kalktı). Eylem çubuğu ticari: "Numune talep et" · "Takibe al"/"Takipte" · 44px "Teklif iste" · yorum sayacı; **ürünlü gönderide beğeni yok** (ürünsüz duyuruda beğeni/yorum/paylaş duruyor). Akışta "Tümü / Bağlantılarım" sekmesi (seçim cihazda `avedon.feedScope` ile hatırlanır). "Favorilerim" her yerde **"Takip Ettiklerim"**, yıldız yerine `bookmark`. Yeni ürün kaydından sonra `confirmAction` ile "Akışta paylaşılsın mı?" (otomatik gönderi yok, düzenlemede sorulmaz). **Tarayıcıda 375px'te doğrulandı:** kart düzeni, Takibe al → Takip Ettiklerim listesine düşüyor, "Bağlantılarım" boş durumu, teklif isteğinde bağlantı sorusu; konsol temiz, `tsc` temiz. **Denenmedi:** telefon; teklif isteğinin bağlantılı durumda sohbet açması; "Akışta paylaş" akışı uçtan uca. **Karar (taslaktan sapma):** "Numune talep et" düğmesinin kutu ikonu düştü — 375px'te dört eylem yan yana sığmıyordu, yazı kırpılıyordu.

- **YENİ TASARIM — 4. VE 5. AŞAMA KODLANDI, TELEFONDA KONTROL BEKLİYOR (2026-09-15):** Kullanıcı "4 ve 5 aşamalara geçelim" dedi. Aynı ekrana iki kez dokunmamak için iki aşama ekran ekran birlikte yapıldı, 7 grupta commit edildi (`ef3739b` ortak parçalar + Ürünler/Ürün/Firma, `58e0a99` Akış + Yorumlar, `09dcbd2` Mesajlar/Sohbet/Yeni Sohbet, `94417f8` Numune takibi/Taleplerim/Gelen Talepler, `c73dfe8` Profil/bağlantılar, son commit Hesaplamalar listesi + Giriş). **4. aşama:** `features/haptics.ts` titreşim sözlüğü (seçim/hafif/başarı/uyarı/hata; basmanın kendisi titreşmez; sık işlemde başarı yok; web'de kapalı), `expo-haptics` (Expo Go SDK 57 paket listesinde var), satır/düğme basılı durumu + Android ripple. **5. aşama:** taslaklara göre (`docs/tasarim-yonleri/C*.dc.html`) kutu yığını yerine beyaz blok + çizgili satır; ortak `ListRow`, `ProductRow`, `SectionHeader`, `SearchField`, `HeaderButton`, `StockIndicator` (100 m altı az stok), `SampleStatusBadge`, `PrimaryButton` (outline, lg, ikon); iskeletler yeni düzene. Backend: akış ürününe `weightGsm/widthCm/stock` (ölçü şeridi). **Doğrulama:** her grup tarayıcıda 375px'te, gerektiğinde geçici test verisiyle (her seferinde silindi): taşma yok, iç içe düğme yok, konsol hatası yok; yazma testleri uçtan uca (satıcı olarak numune adımı ilerletme, bağlantı isteği kabulü → telefonun görünmesi). **Yol üstünde bulunan hatalar:** (1) react-native-web `Alert.alert` boş fonksiyon (kaynakta `static alert() {}`) → web'de gönderi silme, yorum silme, doğrulanmış firma adı değiştirme sessizce çalışmıyordu; `features/confirm.ts` ile düzeldi. (2) Web'de iç içe `<button>` (ürün satırı içinde Talep Et). (3) Yorum silme yalnız uzun basmayla (bulunamaz) → görünür sil düğmesi. **Doğrulanmayan:** Giriş ekranındaki lacivert bloğun yüksekliği tarayıcıda ölçülemedi (kodda 280). **Yeni düzene geçmeyen ekranlar** (taslakta yok): kayıt adımları, formlar (ürün ekle, gönderi paylaş, firma düzenle, numune talep formu), 7 hesaplama formu, Görsel Maliyet, Danışman, Admin, ImageViewerModal. Rehber: `mobile/MOBILE-DESIGN.md`; denetim durumları `mobile/design-baseline.json`'da güncellendi (006, 016, 018 çözüldü; 011, 012, 013, 014 kısmen). **Sıradaki:** telefonda kontrol.

- **EV PC'Sİ KONTROLÜ YAPILDI (2026-09-15):** Ev PC'sindeki oturum raporuna göre orada tek bir commit edilmemiş değişiklik vardı: `docs/yapilacaklar.md`'de GitHub'daki sonraki commit'lerden **eski** bir not (ör. çözülmüş "Render'da kalıcı disk yok" uyarısı). Gönderilmedi (çakışma ve eski bilgiyi geri getirme riski); **silinmedi, `git stash` ile saklandı**. Ev PC'si GitHub'la eşitlendi, açık sunucu yok, ev PC'sinin hafızasına "burada geliştirme yapılmaz, iş PC'sindeki oturumu hatırlat" kuralı yazıldı. İş PC'sinden doğrulandı: GitHub'a ev PC'sinden push gelmedi (`dfb9586`). **Kalan (kullanıcı):** claude.ai/code listesinde çevrimdışı eski oturumları ("Avedon", "Avedon projesini ilk çalıştırma") arşivlemek; kalacak olan çevrimiçi "Avedon Projesi İş Yeri".
- **TELEFON → ANA OTURUM BAĞLANTISI DOĞRULANDI (2026-09-15):** Kullanıcı telefondan "Telefon bağlı" yazdı, mesaj iş PC'sindeki oturuma ulaştı. **Dışarıdan da doğrulandı:** Wi-Fi kapalı, mobil veriyle "Dışarıdan bağlı" mesajı geldi — Remote Control iş ağına değil Anthropic sunucularına bağlandığı için iş PC'si açık ve çevrimiçi olduğu sürece her yerden çalışır. Oturumun adı "Avedon MVP spec" → **"AVEDON — ANA OTURUM (İş PC)"** yapıldı (bu makinede başka oturum yok, arşivde de yok). **Önceki kopmanın sebebi:** sohbet uzayıp özetlenince (compaction) Remote Control uzak oturumu arşivliyor (resmi doküman); çözüm bu PC'de `/remote-control` veya masaüstü uygulamasında Settings > Claude Code > Enable remote control by default. **Bekleyen:** ev PC'sinden claude.ai/code ile aynı oturuma bağlanma denemesi; listede ev PC'sinin eski oturumunun "ESKİ — KULLANMA" diye adlandırılması/arşivlenmesi (kullanıcı yapacak).
- **TEK MAKİNE / TEK OTURUM KARARI (2026-09-15):** Kullanıcı: "render işini tamamlayınca tüm projeyi tek oturumdan yönetelim, ev ve iş pc olayı sorun oluyor." **Karar:** geliştirme yalnız iş PC'sinde (sürekli açık); ev PC'si ve telefon Remote Control ile bu oturuma bağlanır. `CLAUDE.md` buna göre yeniden yazıldı. **Doğrulananlar:** GitHub = iş PC'si (`93109f9`, gönderilmemiş/çekilmemiş commit yok); bekleyen Windows yeniden başlatması yok (son açılış 2026-09-13 00:06); Remote Control dokümanı: uyku/ağ kopmasında kendiliğinden yeniden bağlanır, mesajlar kuyrukta bekler; masaüstü uygulamasında "Settings > Claude Code > Enable remote control by default". **Kullanıcıda kalan (tek seferlik):** (1) ev PC'si açıldığında orada commit edilmemiş iş var mı bakılması (ev oturumu 2026-09-15'te çevrimdışıydı, buradan görülemedi); (2) Windows etkin saatlerinin genişletilmesi (Başlat → Ayarlar → Güncelleştirme ve Güvenlik → Windows Update → Etkin saatleri değiştir; en fazla 18 saat; şu an 08–17) — yeniden başlatma oturumu kapatır ve uzaktan geri açılamaz. **Kabul edilen sınırlama:** Windows yeniden başlarsa uzaktan erişim, biri PC başında uygulamayı açana kadar kesilir (otomatik oturum açma bir güvenlik ayarı, asistan önermez/yapmaz).
- **AÇIK: EVDEN TELEFONDA GELİŞTİRME SÜRÜMÜ TESTİ — ÇÖZÜLMEDİ, İHTİYAÇ DOĞUNCA (2026-09-15):** Evdeki telefon `192.168.1.30`'a (Metro + backend) ulaşamaz. Araştırılan seçenekler (tekrar araştırılmasın): (a) `expo start --tunnel` — `@expo/ngrok` gerekir, hesap gerekmez, yavaş ve zaman zaman kopuyor; Windows'ta antivirüs (bu PC'de AVG) engelleyebiliyor; YALNIZ Metro'yu taşır, backend'i değil. (b) Backend için Cloudflare Quick Tunnel (`cloudflared tunnel --url http://localhost:4000`) — hesap gerekmez ama adres her çalıştırmada değişir (her seferinde `EXPO_PUBLIC_API_URL` + Metro yeniden başlatma), SLA yok, yalnız test amaçlı. (c) Tek tünel: Metro'ya `/api` aktarıcısı + uygulamanın adresi manifest `hostUri`'den türetmesi — `hostUri` manifestte var (`192.168.1.30:8081`, doğrulandı) ama Metro `server.enhanceMiddleware` resmi dokümanda **deprecated** ve yerine yöntem yok → **reddedildi**. (d) Geliştirme sürümünü canlı API'ye bağlamak → deneme verisi gerçek pilot verisine yazılır → **reddedildi**. Şimdilik: evde değişiklikler web önizlemede, telefonda ise iş yerindeyken ya da canlı web sürümünde (avedon-blond.vercel.app, telefon tarayıcısı) denenir. Bu PC'de `cloudflared`, `ngrok`, `@expo/ngrok` kurulu değil.

- **VİDEO (CLOUDFLARE) CANLIDA BAĞLANDI (2026-09-15 14:16):** Render'a `CLOUDFLARE_ACCOUNT_ID` + `CLOUDFLARE_STREAM_API_TOKEN` girildi; `/api/health` → `video.access: ok`, http 200, `tokenFormat: scannable` (53 kr). **Doğrulanan:** kimlik + Stream okuma. **Doğrulanmayan:** yazma/yükleme izni — ilk gerçek video yüklemesi kanıtlayacak (şablon: "Read and write to Cloudflare Stream and Images"). **Yaşanan hata (tekrarlanmasın):** ilk girişte anahtar yerine 32 haneli bir değer (listede görünen anahtar ID'si ya da Account ID) yapıştırıldı → Cloudflare 400 + `9106`. Cloudflare anahtarları `cfut_`/`cfat_` + 40 karakter (+ sağlama) ya da eski biçimde öneksiz 40 karakter; anahtar yalnızca oluşturulduğu an bir kez gösterilir; aynı sayfadaki `curl` deneme komutu kopyalanmamalı. `/api/health` artık `tokenFormat` (scannable/legacy/unrecognized) ve Cloudflare hata kodlarını gösteriyor. **Temizlik (isteğe bağlı):** Cloudflare → API Tokens'ta kullanılmayan eski/deneme anahtarları silinebilir.

- **CANLI VERİ ARTIK KALICI — DOĞRULANDI (2026-09-15 13:39):** Render: Starter plan + 1 GB disk `/var/data` + `DATABASE_URL=file:/var/data/avedon.db` + `JWT_SECRET`. Test: kullanıcı canlıda kayıt oldu (firma Melide Tekstil `cmu2ijc9…f86`) ve 2 fotoğraflı ürün ekledi (`MLD-R0034` 130/160/160, `MLD-Y0023` 1200/180/165); ardından backend'e dokunan `355bcc8` yayını (içinde ondalık migration'ı) canlıya çıktı → firma, iki ürün, değerler birebir ve fotoğraflar (200) yerinde, health `separateDisk: true`. **Önceki yanlış alarmın sebebi:** kullanıcının ilk "bitti"sinde `DATABASE_URL` hâlâ `file:./dev.db` idi. **Render davranışları (doğrulandı):** (1) servisin Root Directory'si `backend` — yalnızca `backend/` altındaki değişiklikler otomatik yayın tetikler, `docs/`-yalnız push yayın yapmaz; (2) başarısız yayında önceki sürüm açık kalır, `status: ok` yeni kodu kanıtlamaz → `/api/health`'teki `commit` alanına bak; (3) disk takılı serviste yayın sırasında kısa 502 normaldir. **Canlıda hâlâ eksik:** `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_STREAM_API_TOKEN` (video), SMS sağlayıcısı (kodlar yalnız Render Logs'ta `[sms]`).

- **ONDALIK HATASI DÜZELTİLDİ (2026-09-15, `355bcc8`, canlıda migration sorunsuz uygulandı):** üç alan `Float`, gösterim `formatMeasure` (220,5 / 1.200,75), form `toInputNumber` (binlik ayraçsız). Veri kopyasında prova + yerel POST/GET/PATCH + tarayıcı kontrolü geçti. Eski kayıt:
- ~~**HATA — ÜRÜNDE ONDALIK DEĞERLER SESSİZCE KESİLİYOR (2026-09-15, düzeltilmedi):**~~ `Product.stock`, `weightGsm`, `widthCm` şemada `Int`; form `220,5` gibi değeri kabul edip `220.5` gönderiyor, sunucu `201` dönüyor ama `220` kaydediliyor (yerel HTTP testiyle doğrulandı: 220.5→220, 150.5→150). Kullanıcı hata görmeden veri kaybediyor. Düzeltme: üç alanı `Float`'a çeviren migration (SQLite'ta tablo yeniden kurulur; Render'da kalıcı disk üzerinde açılışta `migrate deploy` ile uygulanır) + mobil gösterimde ondalık biçimleme. Canlı kalıcılık testi bitene kadar push edilmedi (push yeniden yayın tetikler).
- **KÖK NEDEN BULUNDU (2026-09-15 13:02):** yeni `/api/health` canlıda `commit: 38218fe`, `databasePath: /opt/render/project/src/backend/prisma/dev.db`, `separateDisk: false` döndü → Render'daki `DATABASE_URL` hâlâ `file:./dev.db` (değişiklik kaydedilmemiş / yanlış yere girilmiş / "Save only" seçilmiş olabilir). Kullanıcıdan değeri `file:/var/data/avedon.db` yapıp "Save, rebuild, and deploy" demesi istendi. Doğru olduğunda health `databasePath: /var/data/avedon.db` ve **`separateDisk: true`** göstermeli; `false` ise disk takılı değil demektir.
- **(çözüldü — en üstteki "CANLI VERİ ARTIK KALICI" kaydı) CANLI KALICILIK TESTİ BAŞARISIZ (2026-09-15, kök neden aranıyor):** Not: kullanıcının ürün kaydı sorunu da çözüldü; ikinci denemede iki ürün fotoğraflarıyla kaydedildi. Kullanıcı disk + `DATABASE_URL` adımlarını "bitti" dedi. Canlıda kayıt oldu (firma "Melide Tekstil", 09:45 UTC oluştu, aramada görünüyordu); **"Deploy latest commit" sonrası firma kayboldu** (a/M/T/e aramalarının hepsi boş). Yani veri hâlâ geçici dosya sistemine yazılıyor. **Yanlış çıkarım düzeltmesi:** önceki "ürün listesi boş → veritabanı yeni diskte" kanıt değildi; geçici yerde sıfırdan açılan veritabanı da `RENDER=true` seed atlaması yüzünden boş görünür. Adaylar: (1) Render'da `DATABASE_URL` kaydedilmedi/eski değerde, (2) disk eklenmedi veya farklı yola bağlı. Teşhis için `/api/health` artık `commit` (RENDER_GIT_COMMIT) ve `storage` (`databasePath`, `databaseExists`, `separateDisk` — klasörün kökle aynı aygıtta olup olmadığı) döndürüyor; gizli bilgi yok. Ayrıca: kullanıcının eklediği ürün hiç yazılmamıştı; yerelde gerçek HTTP testi fotoğrafsız 201, fotoğraflı 201, firmasız 403 `no_company` — sunucu doğru; kullanıcıdan kaydet anında ne gördüğü soruldu.

- **CANLI BACKEND 2026-09-13'TEN BERİ ESKİ SÜRÜMDEYDİ — DÜZELTİLDİ (2026-09-15):** Render'a hiç `JWT_SECRET` girilmemişti; gerçek kimlik doğrulama commit'inden (`911eccf`) beri `auth.ts` açılışta hata fırlatıyor, **her yayın başarısız oluyordu**. Render başarısız yayında önceki sürümü açık tuttuğu için `/api/health` 200 dönüyor, site "çalışıyor" görünüyordu; oysa canlıda ilk MVP vardı (`/api/posts` → 404). Kullanıcı Render → Environment'a `JWT_SECRET` ekledi (değer bu PC'de ekrana basılmadan üretildi, sohbete girmedi, kullanımdan sonra dosya silindi). Doğrulandı: `/api/posts` → **401** (hafta sonu kodu canlıda). **Ders:** `/api/health` tek başına canlı sürümü kanıtlamaz — yeni bir uçla kontrol et. **Canlıda hâlâ eksik:** `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_STREAM_API_TOKEN` (video "etkinleştirilmedi" der). Bir sonraki adım: kart → Starter → disk → `DATABASE_URL` (aşağıdaki kayıt).

- **(tamamlandı, en üstteki kayda bakın) CANLI VERİTABANI — KARAR: POSTGRES DEĞİL, RENDER KALICI DİSK + SQLITE (2026-09-15):** Render dokümanıyla doğrulanan kök neden: ücretsiz web servisinin dosya sistemi geçici; SQLite verisi yalnız yeniden yayında değil **15 dk ziyaretsiz kalıp uykuya geçince de siliniyor**, uyanması ~1 dk. Seçenekler: (A) Postgres — ücretsiz Render Postgres **30 gün sonra siliniyor** (+14 gün), yani ücretli olmalı; üstelik kod değişikliği, iki makinede yerel geliştirmenin de Postgres'e geçmesi ve ücretsiz sunucuda 1 dk soğuk açılış sürer. (B) **Ücretli web servisi + kalıcı disk** — kod ve komutlar değişmiyor, soğuk açılış bitiyor, disk her 24 saatte otomatik yedekleniyor (en az 7 gün saklanıyor). **B seçildi.** Bedeli: disk takılı serviste kesintisiz yayın yok (yayında kısa kesinti) ve tek sunucuya ölçeklenemiyor — pilot için sorun değil; büyüyünce Postgres'e geçilir. Mevcut Render ayarları (eski kayıttan): Build `npm install && npx prisma generate && npm run build`, Start `npx prisma migrate deploy && npm run seed && npm start` — migration zaten açılışta çalışıyor, disk de açılışta erişilebilir (build/pre-deploy'da değil), komut değişmiyor. Kod: `seed.ts` canlıda (`RENDER=true`) yalnız admin kuruyor, örnek firma eklemiyor. **Kullanıcı adımları:** (1) avedon-backend → Settings → Instance Type → en küçük ücretli; (2) Disks → Mount Path `/var/data`, Size 1 GB (büyütülür, küçültülemez) → Add disk; (3) Environment → `DATABASE_URL` = `file:/var/data/avedon.db` → kaydet. **Sonra doğrulama:** canlıda kayıt ekle → elle yeniden yayın → kayıt duruyor mu. **Canlıya ayrıca girilmesi gerekenler:** `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_STREAM_API_TOKEN` (video için).

- **İŞ PC'Sİ HİZALANDI (2026-09-15):** `git pull` (2d1cb8d), 3 yeni migration + `prisma generate`, mobil `npm install`, iki tarafta `tsc` temiz. `mobile/.env` → `http://192.168.1.30:4000/api` (iş PC'si **kablolu/Ethernet**, telefon aynı yerel ağda olmalı). Backend ve Metro `192.168.1.30` üzerinden yanıt veriyor. **Hâlâ kullanıcıda:** (1) `CLOUDFLARE_ACCOUNT_ID` / `CLOUDFLARE_STREAM_API_TOKEN` iş PC'sinin `backend/.env`'inde boş — ev PC'sinden taşınana kadar video "etkinleştirilmedi" der; ~~(2) telefonun iş PC'sine bağlanabildiği denenmedi~~ → **2026-09-15'te denendi, ÇALIŞIYOR:** iş Wi-Fi'sindeki telefon Expo Go ile `exp://192.168.1.30:8081` üzerinden açıldı, admin ile giriş yapıldı, Akış geldi. İş ağı cihazları ayırmıyor, ağ profili "Özel", güvenlik duvarı ayarı gerekmedi (evdeki sorun burada yok). **İpucu:** telefonun Chrome'unda adresi `http://` olmadan yazınca Chrome `https`'e çeviriyor ve `ERR_SSL_PROTOCOL_ERROR` veriyor — bu ağ engeli değil, adres başına `http://` yazılmalı. İş PC'si "ana makine" olarak sürekli açık kalacak; telefondan Remote Control ile bağlanılıyor.
- **(eski kayıt) İŞ PC'SİNE GEÇİŞ (2026-09-15) — İLK AÇILIŞTA BUNLAR YAPILMALI:** Kullanıcı ev PC'sinden iş PC'sine geçti. Ev PC'sinde yapılan ve iş PC'sinde henüz olmayan her şey git'te; git'te OLMAYANLAR aşağıda. PowerShell'de script çalıştırma kapalı: komutlar `npm.cmd` / `npx.cmd` ile.
  1. `git pull`
  2. `backend` klasöründe: `npm.cmd install`, sonra `npx.cmd prisma migrate deploy` (yeni migration'lar: `add_sample_request_timeline`, `add_videos`, `add_post_edited_at`, `add_company_logo`), sonra `npx.cmd prisma generate`. Veritabanı iş PC'sinde hiç yoksa ayrıca `npm.cmd run seed`.
  3. `mobile` klasöründe: `npm.cmd install` (yeni paketler: IBM Plex yazı tipleri, `expo-font`, `expo-splash-screen`, `expo-video`, `expo-file-system`).
  4. **`backend/.env`** (`backend/.env.example` şablonuna bakın): `DATABASE_URL`, `JWT_SECRET`, `ANTHROPIC_API_KEY`, **`CLOUDFLARE_ACCOUNT_ID` ve `CLOUDFLARE_STREAM_API_TOKEN`** (bu ikisi 2026-09-14'te eklendi, iş PC'sinde büyük ihtimalle yok). Değerler ev PC'sindeki `backend/.env`'den kullanıcı tarafından taşınmalı (USB / şifre yöneticisi); **sohbete yapıştırılmamalı**. Taşınamazsa Cloudflare panelinde aynı şablonla yeni token üretilebilir.
  5. **`mobile/.env`**: `EXPO_PUBLIC_API_URL=http://<İŞ PC'sinin IPv4 adresi>:4000/api` — ev PC'sinin adresi (192.168.1.103) iş yerinde çalışmaz; `ipconfig` ile bulunur.
  6. Başlatma: `backend`'de `npm.cmd run dev`, `mobile`'da `npx.cmd expo start --lan`. **`CI=1` ile başlatmayın:** o kipte Metro dosya değişikliklerini izlemiyor ve eski paketi sunuyor (2026-09-15'te bu yüzden yeni kod telefona gitmedi).
  7. **Bilinen riskler:** (a) İş yeri Wi-Fi'si cihazları birbirinden ayırıyorsa ya da güvenlik duvarı `node.exe`'yi engelliyorsa telefon bağlanamaz ("Something went wrong"); Windows ilk çalıştırmada izin sorarsa izin verin. (b) **Veritabanları ayrı:** ev PC'sindeki gönderiler, video gönderisi ("Sandığımıza…"), "Mm" firması ve logosu iş PC'sinde yok; o testler ev PC'sinde ya da canlı ortamda yapılmalı. (c) Asistanın hafızası (`~/.claude`) makineye özel; kalıcı kurallar bu yüzden `CLAUDE.md`'de.

- **YENİ TASARIM — 3. AŞAMA KODLANDI, TELEFONDA KONTROL BEKLİYOR (2026-09-15):** 1–2. aşama (IBM Plex yazı tipi, C renkleri, 6px köşeler, lacivert üst bant) telefonda kontrol edildi, kullanıcı onayladı: "yazı yeni tip, evet rahat okunuyor, sorun görünmüyor". 3. aşama (durumlar) eklendi: ortak `components/Skeleton.tsx` (tek animasyonlu iskelet, 150 ms gecikme, "hareketi azalt" açıksa sabit; taslak `CYukleniyor.dc.html`), `components/StateView.tsx` (`EmptyState` / `ErrorState` "Tekrar dene" / `InlineError` şeridi, 404'ü boş durum sayan `isNotFound`, ağ hatasını anlaşılır metne çeviren `friendlyMessage`), `components/refresh.tsx` (lacivert aşağı çekip yenileme), `features/useFocusLoad.ts` (iskelet yalnızca elde veri yokken; geri dönünce sessiz yenileme). 15 ekrana uygulandı: Akış, Ürünler, Mesajlar, Sohbet, Yorumlar, Yeni Sohbet, Bağlantılar, Bağlantı İstekleri, Taleplerim, Gelen Talepler, Numune Takibi, Ürün Sayfası, Firma, Profil, Profilim, Admin. **Yol üstünde düzeltilen hatalar:** (1) Akış'taki 30 sn yenileme kısıtlaması hiç çalışmıyordu (kapanışta `posts` hep boş görünüyordu), her sekme dönüşünde liste başa sarıyordu; (2) alt ekrandan geri dönünce listeler/firma/yorumlar bir an kaybolup yükleniyor çemberine dönüyordu; (3) Yeni Sohbet'te sohbet başlatma hatası liste doluyken hiç görünmüyordu; (4) başarılı yüklemeden sonra eski hata mesajı ekranda kalıyordu; (5) Firma sayfasında başlığın altında fazladan boşluk (`edges` 'top'); (6) Profilim'de yükleme sürerken Çıkış dahil menü görünmüyordu; (7) Mesajlar listesindeki okunmamış rozeti yeni kırmızı (`notification`) renge alındı. tsc temiz, `expo export` ile tam Android paketi başarılı. **Sıradaki:** telefonda kontrol → 4. aşama basma geri bildirimi → 5. aşama ekran düzenleri.

- **YENİ TASARIM (C · PAZAR MASASI) ONAYLANDI (2026-09-14), UYGULAMAYA BAŞLANIYOR:** Kullanıcı "Yeni tasarımı onaylıyorum, düzenleyebilirsin" dedi. Taslak: https://claude.ai/code/artifact/2a3759b3-2620-4d87-8ccb-c35dd01bbdae, kaynak `docs/tasarim-yonleri/`. Aşamalar (mobile-redesign-app yöntemi, her aşama sonunda telefonda kontrol): 1 yazı tipi (IBM Plex Sans + Mono) ve boşluklar, 2 renkler ve lacivert üst bant, 3 yükleme/boş/yenileme durumları (iskelet), 4 basma geri bildirimi, 5 ekran düzenleri (kutu yığını yerine çizgili listeler, ürün/akış/firma/mesaj/profil/takip/hesaplama). Navigasyon değişmiyor. Butonlar hap değil 6px köşeli. Tamamlanınca `MOBILE-DESIGN.md` yazılacak ve `mobile/design-baseline.json` karşılaştırılacak.
- **HESAPLAMALAR — KULLANICI DOĞRULADI (2026-09-15):** "hesapları kontrol ettim, doğru." **Açık:** kullanıcının "görsel grafik ile alakalı bazı istekleri" olacak, henüz iletmedi ("şimdi değil").
- **(eski kayıt) HESAPLAMALAR — KULLANICI DOĞRULUĞU YARIN KONTROL EDECEK (2026-09-15):** "Doğruluğunu bugün yapmayacağım, yarın bilgi veririm." Formüllere o zamana kadar dokunulmuyor. Aynı gün kullanıcı isteğiyle **iplik giriş tabloları küçültüldü:** her iplik ayrı kutuda alt alta alanlar yerine tek satır = tek iplik (`TableInput`, dokundukça birim değiştiren `UnitToggle`); İplik Kullanım Oranı, Kumaş Üretimi ve Maliyet ekranlarında. Üretimde iplik payları sonuç kartına taşındı.

- **FİRMA LOGOSU VE FİRMA BİLGİLERİNİ DÜZENLEME EKLENDİ (2026-09-14), CİHAZDA TEST BEKLİYOR:** Kullanıcı akıştaki mavi "M"nin ne olduğunu sordu (firma adı "Mm"nin baş harfi, logo yerine duran geçici işaret). Eklenenler: logo **ayrı `CompanyLogo` tablosunda** (giriş yanıtı, kullanıcı profili ve firma araması firmayı `include: { company: true }` ile çektiği için logo Company'de olsaydı her birinde taşınırdı), `Company.logoUpdatedAt` hem "logo var mı" hem önbellek anahtarı (logo değişince telefondaki eski logo takılı kalmaz). Uçlar: `GET /api/companies/:id/logo`, `PATCH /api/companies/:id` (yalnız o firmanın çalışanı; ad, hakkında, iletişim e-postası/telefonu, logo; vergi no ve şirket kodu düzenlenemez). **Kural:** doğrulanmış firma adını değiştirirse doğrulama "inceleniyor"a düşer; telefon kaydetmeden önce uyarıyor. Mobil: `CompanyAvatar` logo gösteriyor (akış, ürün sayfası, numune takibi, firma sayfası), yeni **Firmayı Düzenle** ekranı, firma sayfasında artık **Hakkında ve iletişim bilgileri görünüyor** (önceden hiç gösterilmiyordu). FINDING-018 düzeltildi: başka firmanın sayfasında başlık artık firmanın adı. Kullanıcının firma adı "Mm" deneme amaçlı olabilir; bu ekrandan kendisi düzeltecek.

- **GÖNDERİ DÜZENLEME EKLENDİ (2026-09-14), CİHAZDA TEST BEKLİYOR:** Kullanıcı "Sandığımıza" yerine "Standımıza" yazmak istediğini, paylaşımdan sonra yalnızca Sil olduğunu bildirdi. Eklenenler: `Post.editedAt` (migration `add_post_edited_at`), `GET /api/posts/:id` (akışla aynı görünürlük kuralı), `PATCH /api/posts/:id` (yalnız yazar; yazı, görünürlük, ürün; ürün yine yalnız kendi firmasından; hiçbir şey değişmezse editedAt konmaz; beğeni/yorum korunur). **Fotoğraf ve video bilinçli olarak düzenlenemez** (şema `.strict()` reddeder), ekranda bu açıkça yazıyor. Mobil: akışta kendi gönderinde Düzenle + Sil, meta satırında "düzenlendi", paylaşım ekranı `postId` ile düzenleme modunda açılıyor. 18 uçtan uca kontrol geçti. Kullanıcının "Standımıza" düzeltmesini bu özellikle kendisi yapması bekleniyor.
- **Videonun gönderiye bağlandığı doğrulandı:** Stream'de hazır (52,55 sn), imzalı HLS listesi ve küçük resim 200 dönüyor.

- **HESAPLAMALAR ÖRME PARKURU'NA GÖRE YENİLENDİ (2026-09-14), CİHAZDA TEST BEKLİYOR:** Kullanıcı referans olarak https://www.youtube.com/@ormeparkuru kanalını gösterdi (8 kısa video; site ormeparkuru.com, araçlar ücretli üyelik arkasında). Videolar izlenemiyor ve otomatik altyazı erişim anahtarı olmadan gelmiyor; ekranlar **video kapaklarından ve YouTube önizleme karelerinden** (WebP, Edge ile 5x büyütülerek) okundu. Yenilenenler: gramaj (numuneden + örgüden tahmin, K faktörü kaldırıldı), iplik numarası (dtex, katlı iplik, numuneden), **iplik kullanım oranı (yeni ekran)**, kumaş üretimi (iğne, devir, randıman, iplik satırları; isteğe bağlı fason geliri), maliyet ve satış fiyatı (çok iplik ₺/$/€, örme/boya fasonu, gider, boya firesi, kâr; ham/boyalı maliyet ve satış). 31 formül testi geçti; iki sonuç referans ekranla sağlandı. **Boya firesi kullanıcı tarafından teyit edildi:** boya fasonu ham kilo üzerinden alınır, maliyet fire düşülmüş boyalı kiloya bölünür ("100 kg ham, %8 fire, 92 kg boyalı"); mevcut formül birebir bu. Kur internetten çekilmiyor, kullanıcı giriyor.
- **VİDEO YÜKLEME HATASI BULUNDU VE DÜZELTİLDİ (2026-09-14):** İlk gerçek denemede 153 MB / 52,5 sn video Cloudflare'de hazırdı ama uygulama hata sandı (expo-file-system Android okuma süresi 60 sn) ve gönderi ("Sandığımıza sizleri bekliyoruz") videosuz paylaşıldı. Düzeltme: hata olsa da sunucudan Cloudflare durumunu doğrulama, başarısızlıkta silme, 190 MB üstünü seçimde durdurma. **Açık:** (1) ~~o video sahipsiz duruyor~~ → kullanıcının isteğiyle "Sandığımıza sizleri bekliyoruz" gönderisine veritabanından bağlandı. (2) 200 MB üstü için parça parça (tus) yükleme yok; yüksek çözünürlüklü telefon videoları bu sınıra takılabilir. (3) Düzeltme cihazda henüz denenmedi.

- **VİDEO — CLOUDFLARE BAĞLANDI, TELEFONDA İLK GERÇEK DENEME BEKLENİYOR (2026-09-14):** Kullanıcı Cloudflare hesabını açtı, **Starter Bundle (aylık 5$)** satın aldı: 1.000 dk video deposu, 5.000 dk izlenme, 100.000 fotoğraf deposu dahil. Stream paneli sol menüde **Build → Images & Stream** altında (abonelik: **Plans**). API anahtarı "Read and write to Cloudflare Stream and Images" şablonuyla oluşturuldu (Stream Read/Edit, Images Read/Edit, Account Analytics Read; IP filtresi ve süre sınırı bilinçli olarak boş). Anahtar `/user/tokens/verify` ile aktif, hesap numarası `/stream` listesiyle doğrulandı, depo sınırı 1.000 dk görünüyor. `.env` bu makinede dolu. **İş bilgisayarının `backend/.env`'ine de aynı iki değer girilmeli; canlıya çıkarken Render'a da.** Not: kullanıcı ilk yapıştırmada Account ID satırına Cloudflare'in anahtar deneme komutunun bir parçasını koymuştu; Account ID dashboard adres çubuğundaki 32 karakterlik dizi.
- **(önceki kayıt) VİDEO — 1. AŞAMA (AKIŞ GÖNDERİLERİ) KODLANDI (2026-09-14):** Sunucu: `Video` modeli + migration `add_videos`, `src/stream.ts` (Cloudflare istemcisi; uç yolları ve `result.token` API referansından doğrulandı), `src/videos.ts` (durum tazeleme, erişim kontrolü, silme), `routes/videos.ts` (`POST /upload-url`, `GET /:id`, `GET /:id/playback`, `DELETE /:id`), gönderiye `videoId` bağlama ve gönderi silinince Stream'den silme. Sahte Cloudflare ile 38 uçtan uca kontrol geçti (`DELETE /:id` ucu bu testten sonra eklendi, henüz testte yok). Mobil: `expo-video` + `expo-file-system` kuruldu (Expo Go'da çalışır), `features/videoUpload.ts` (galeriden seçim, 60 sn kontrolü, doğrudan Cloudflare'e yükleme + ilerleme), `components/PostVideo.tsx` (işleniyor/hata/küçük resim + oynat), gönderi ekranında "Video Ekle". `.env`'de `CLOUDFLARE_ACCOUNT_ID` ve `CLOUDFLARE_STREAM_API_TOKEN` boş yer tutucu olarak duruyor; boşken video düğmesi "henüz etkinleştirilmedi" diyor. **Sıradaki:** kullanıcı anahtarları girince gerçek hesapla telefonda deneme; sonra 2. aşama Ürün sayfası, 3. aşama Sohbet. **Canlıya çıkmadan önce:** Render'da da iki anahtarın girilmesi ve istenirse webhook eklenmesi. **Bilinen açık:** paylaşılmadan uygulama kapatılırsa yüklenen video sahipsiz kalabilir (ekrandan normal çıkışta siliniyor); periyodik temizlik gerekebilir.
- **VİDEO — KARARLAR VERİLDİ (2026-09-14):** servis **Cloudflare Stream** (1.000 dk depo aylık 5$ ön ödemeli, izlenen 1.000 dk 1$, kodlama ücretsiz, ücretsiz plan yok); süre sınırı **60 saniye**; kapsam **Akış gönderileri + Ürün sayfası + Sohbet mesajları** (üçü de). Bağlantılara özel gönderiler ve sohbet videoları herkese açık bağlantıyla izlenememeli, yani imzalı oynatma gerekiyor. **Kullanıcının yapacağı:** Cloudflare hesabı açmak, Stream'i etkinleştirip depo satın almak ve API anahtarı üretmek (ödeme ve hesap işlemleri asistan tarafından yapılamaz). İlk kayıt:
- **(ilk kayıt) GÖNDERİYE VİDEO EKLEME — KULLANICI İSTEDİ (2026-09-14):** "Akış'taki ekle butonu sadece resim bekliyor, video ilavesi yapılabilir mi; bazı anlatımlar ve görseller önemli." Teknik durum: `expo-image-picker` (`mediaTypes: ['images','videos']`, sonuçta `duration`/`fileSize`/`mimeType`) ve oynatma için `expo-video` SDK 57'de **Expo Go'ya dahil** — yeniden derleme gerekmiyor. Engel saklama: bugünkü base64-in-SQLite yöntemi (`express.json` 15 MB sınırı, fotoğraf ~200 KB) video için uygun değil; Android'de cihaz üstü video sıkıştırma Expo Go'da yok (`videoExportPreset`/`videoQuality` yalnızca iOS). **Nesne depolama servisi gerekiyor ve hesabı kullanıcı açmalı** (asistan hesap açamaz). Seçenekler: Cloudflare Stream (sıkıştırma + uyarlanır oynatma, ücretli), Cloudflare R2 (ham dosya deposu), Supabase Storage. Kararlar: servis, video süre sınırı, videonun yalnızca gönderilerde mi ürün sayfasında da mı olacağı. Not: fotoğraflar da aynı depoya taşınırsa veritabanı şişmesi sorunu da kökten çözülür. Ayrıca repoda Render dağıtım ayarı yok; canlı ortamın SQLite dosyasını kalıcı diskte tutup tutmadığı doğrulanmadı.

- **YENİDEN TASARIM — YÖN SEÇİLDİ: C · PAZAR MASASI (2026-09-14).** Kullanıcı "3 model daha güzel" dedi. Sıradaki adım: C yönünü ana ekranların tamamına taslak olarak genişletmek (Ürünler, Mesajlar, Sohbet, Profil, Numune takibi, Firma, Hesaplamalar, Giriş) ve onay almak; onaydan sonra `MOBILE-DESIGN.md` yazılıp aşamalı uygulamaya geçilecek. Not: C'de butonlar hap değil 6px köşeli; bu, bir önceki turda onaylanan hap butonların yerini alıyor (kullanıcı C'yi bu hâliyle gördü ve seçti). Seçim öncesi kayıt:
- **(eski kayıt) YENİDEN TASARIM — KULLANICININ YÖN SEÇMESİ BEKLENİYOR (2026-09-14):** Kullanıcı "baştan modern bir yeniden tasarım" istedi (önce görsel taslak + onay, sonra kod). Denetim yapıldı: `mobile/design-baseline.json` (17 bulgu, kritik yok; en zayıf alan yükleme/boş/yenileme durumları, ikinci büyük sorun her ekranın "gri zemin + beyaz kutu" olması). Üç yön aynı iki ekranda (Akış + Ürün sayfası) çizildi ve tuvale konuldu: https://claude.ai/code/artifact/2a3759b3-2620-4d87-8ccb-c35dd01bbdae — **A Rafine Lacivert** (mevcut lacivert/mavi, Figtree, kutu yerine ince çizgi), **B Ham Kumaş** (ham bez zemin, kök boya vurgu, Newsreader + Instrument Sans, kumaş etiketi imza öğesi), **C Pazar Masası** (yoğun B2B, IBM Plex Sans + Mono, ölçüler akışta). Taslak kaynakları `docs/tasarim-yonleri/`. **Seçim gelince:** seçilen yön tüm ekranlara taslak olarak genişletilir, onaylanınca sırayla uygulanır (1 yazı/boşluk, 2 renk + koyu mod, 3 durumlar, 4 basma geri bildirimi, 5 ekran düzenleri). Navigasyon değişmiyor, denetim gerek görmedi.

- ~~Telefonda test edilmeyi bekliyor~~ → **2026-09-13 tarihinde gerçek cihazda test edildi ve geçti** (aşağıda Tamamlananlar'a bakın).
- ~~Navigasyon yeniden yapılandırması~~ → **2026-09-13'te yapıldı ve cihazda test edildi** (Tamamlananlar'a bakın).
- **Google ile giriş — KARAR VERİLDİ, ön hazırlık kullanıcıda:** Kullanıcı Google ile girişin büyük kolaylık sağladığını belirtti ve tasarımda da var (`2.1 Login Screen`: Google / Facebook / LinkedIn). **Karar:** telefon + SMS akışı kalacak, Google ile giriş **üzerine eklenecek**; Google'la giren yeni kullanıcıdan bir kez telefon doğrulaması istenecek. Gerekçe: kimlik modeli telefon merkezli (WhatsApp bildirimleri, "bağlantı kurunca telefon görünür" kuralı, `User.phone` unique). **Başlamadan önce kullanıcının Google Cloud Console'da bir OAuth istemcisi oluşturup client ID'yi vermesi gerekiyor** — asistan hesap açamaz/kimlik bilgisi giremez.
- **Tasarımdan çıkan, henüz uygulanmamış farklar** (bkz. `docs/tasarim-envanteri.md`): (1) Tasarımda mesajlaşma bir sekme değil, **üst barda rozetli ikon**; 5. sekme **Takvim**. Bizde Mesajlar sekme (keşfet/takvim olmadığı için bilinçli). (2) Butonlar/input'lar tasarımda **tam yuvarlak (hap)**, bizde `radius.md = 12`. (3) Üst barda arama kutusu + logo yok. (4) Tasarım dosyasındaki 129 ekranın görseli henüz dışa aktarılmadı (`Dosya → Dışa Aktar → Tüm Artboard'lar`).
- **Güvenlik/olgunluk boşlukları (bilinçli olarak ertelendi):** (1) Uygulamada hiçbir yerde hız sınırlama (rate limiting) yok — akış, bir kullanıcının yazdığının herkese yayıldığı ilk özellik olduğu için bu eksiklik ilk kez burada anlam kazanıyor. (2) Şikayet/engelleme mekanizması yok; tek moderasyon imkânı gönderi sahibinin kendi gönderisindeki yorumu silebilmesi. (3) `GET /api/connections` tam `User` satırı dönüyor, telefon numarası dahil — bağlantılar arasında savunulabilir ama daraltılmalı. (4) Beğeni/yorum bildirimi yok; doğal olarak sekme rozetleriyle birlikte, navigasyon yeniden yapılandırmasında ele alınmalı. (5) `Company.logoUrl` alanı yok — akış kartlarında firma logosu yerine baş harf gösteriliyor.
- ~~Tasarım dosyaları incelenmeyi bekliyor~~ → **ikisi de incelendi.** PDF (9 ekran) ve XD dosyası (132 ekran) çözümlendi; bulgular `docs/tasarim-envanteri.md`, ham ekran metinleri `docs/xd-ekran-metinleri.md`. **`.xd` dosyası bir ZIP arşivi** — Adobe XD olmadan `unzip` + `docs/xd-extract.js` ile okunabiliyor.
- **Telefon–bilgisayar bağlantısı kalıcı olarak çözülmedi (2026-09-14):** "Something went wrong" yine çıktı; telefon Metro'ya hiç ulaşamadı (kod hatası değil). İki engel tespit edildi: (1) ev Wi-Fi'si (`FiberHGW_ZT57YQ`) Windows'ta **"Genel ağ"** olarak kayıtlı ve Genel profilde `C:\program files\nodejs\node.exe`'yi **engelleyen iki güvenlik duvarı kuralı** var — Windows'ta engelleme kuralı izin kuralını geçersiz kıldığı için eklediğimiz "Avedon Backend / Avedon Expo Metro" izinleri işlemiyor; (2) AVG güvenlik duvarı (`afwServ`) açık. AVG geçici kapatılınca çalıştı, ama ağ profili hâlâ Genel. **Kalıcı çözüm kullanıcıda:** Ayarlar → Wi-Fi → ağ özellikleri → "Özel ağ" + AVG'de ev ağını güvenilir işaretlemek. Asistan güvenlik ayarlarını kendisi değiştirmez.
- **Gerçek SMS sağlayıcısı henüz yok:** OTP kodları şu an sadece backend konsoluna yazdırılıyor (geliştirme modu). Yayına çıkmadan önce Twilio gibi bir sağlayıcı eklenmeli (`backend/src/sms.ts` içinde yorum olarak nasıl ekleneceği yazılı).

## Sonraki Özellik Kararı

Gerçek kimlik doğrulama tamamlandı (bkz. Tamamlananlar).

**2020 vizyonu (Takyon Ai PDF) — KARAR VERİLDİ (2026-09-13):** PDF, Avedon'u feed/hikayeler/keşfet/mesajlaşma/bağlantı-network sistemi olan tam bir sosyal medya platformu olarak tanımlıyor (bkz. `Takyon Ai - Avedon Product.pdf`, 9 ekran). Kullanıcı **"Evet"** diyerek bu tam vizyonu (sosyal ağ + B2B ticaret hibriti) hedef olarak onayladı — seçici/kısmi yaklaşım değil, tam kapsam.

**Aşamalı yol haritası (bağımlılık sırasına göre):**
1. **Bağlantı/Network sistemi + zenginleştirilmiş profil sayfası** — PDF'e göre mesajlaşma ve birçok UI durumu ("bağlantı kur" vs "mesaj gönder") buna bağlı, bu yüzden temel/ilk aşama.
2. **Mesajlaşma** — bağlantı sistemine bağımlı.
3. **İçerik akışı (feed)** — gönderi paylaşma, beğeni, yorum.
4. **Hikayeler** (24 saatlik içerik).
5. **Keşfet / haber akışı** (otomatik içerik toplama, kişiselleştirme).
6. **Kategori kataloğu** genişletmesi (üretici/konfeksiyoncu/boyahane vb. alt kategoriler).
7. **Numune sürecini zaman damgalı timeline'a dönüştürme.**
8. **Firma sayfasını zenginleştirme** (çalışan listesi, sertifikalar, ofis görselleri).

**1., 2. ve 3. aşama + navigasyon yeniden yapılandırması tamamlandı ve cihazda test edildi.**

**XD tasarımı incelendikten sonra güncellenen öncelik önerisi** (bkz. `docs/tasarim-envanteri.md` sonuç bölümü) — sıradaki adaylar, etki/maliyet sırasına göre:
1. ~~**Numune takibini zaman damgalı timeline'a çevirmek**~~ → **2026-09-13'te yapıldı** (Tamamlananlar'a bakın). **Cihazda test edilmeyi bekliyor.**
2. ~~**Ürün detay sayfası**~~ → **2026-09-13'te yapıldı** (Tamamlananlar'a bakın). Tasarımdan **bilinçli olarak ertelenen** parçaları: (a) **favori/yıldız** — `Favorite` modeli gerektiriyor, ayrı iş; (b) **ürün bazında görünürlük** ("Herkese Açık / Sadece Size Açık") — şu an tüm katalog herkese açık, `GET /api/products` kimlik doğrulaması bile istemiyor; (c) alt çubuktaki **telefon ve mesaj ikonları** — telefon numarası gizlilik modelimizde ancak bağlantı kurulunca görünüyor, mesajlaşma da kabul edilmiş bağlantı istiyor; ikisi de çoğu ziyaretçide çalışmayan düğme olurdu, yerine "Firma Sayfasını Aç" kondu; (d) fotoğrafta **parmakla yakınlaştırma** — `react-native-gesture-handler` bağımlılığı gerektiriyor, tam ekran görüntüleme şimdilik yeterli.
3. ~~**Görsel dil geçişi**~~ → **2026-09-13'te yapıldı** (Tamamlananlar'a bakın).
4. **Ürün sayfasına MOQ (minimum sipariş miktarı) + termin süresi** — kullanıcı kararı (2026-09-13): **MOQ ve termin görünsün, fiyat görünmesin**; tekstilde fiyat pazarlıkla belirleniyor. B2B araştırmasında da alıcının ilk sorduğu iki bilgi bunlar. `Product` modeline iki alan (`moqMeters`, `leadTimeDays`) + ürün formu + özellikler kartı satırı gerekiyor.
5. **Kategori ağacı** (6. madde): taksonomi tasarımdan hazır çıktı (30+ örme tipi, sektör ayrımı, kullanım amacı). Ürün arama/filtrelemeyi gerçekten kullanılır yapar.
6. **Firma sayfası alanları** (8. madde): kuruluş yılı, firma tipi, çalışan sayısı, ana pazarlar, sertifikalar, ofis görselleri.
7. **Koyu mod** — 2026 araştırmasında "opsiyonel özellik" sayılmıyor; tasarımda da bizde de yok. Tema dosyası merkezî olduğu için maliyeti orta ama her ekranın iki hâlinin gözle test edilmesi gerekiyor. Kullanıcı görsel dil turunun dışında tutmayı seçti (2026-09-13).
8. **Google ile giriş** (yukarıdaki açık iş; kullanıcının Google Cloud hazırlığı gerekiyor).
9. Hikayeler / Takvim — vizyonun sosyal katmanı. (Keşfet kullanıcı kararıyla yol haritasından çıkarıldı, yerini Hesaplamalar sekmesi aldı. Hikayeler görsel dil turunda **ertelendi**: B2B araştırması keşiften çok hızlı sipariş ve bilgi netliğinin kazandığını söylüyor.)
10. **Abonelik/ödeme** — iş modeli kararı gerektiriyor (3 ay ücretsiz + aylık abonelik + ilk 10 ürün ücretsiz).

Diğer, vizyonla ilgisiz adaylar (bkz. `docs/durum.md`):
- Ödeme/fatura akışı (hiç yok)
- WhatsApp webhook'unu platform içi mesajlaşmaya bağlama
- Mobile `Company` tipi ile Prisma şeması arasındaki `productCategories`/`employeeIds` tutarsızlığını gidermek

## Tamamlananlar (kısa özet)

- 2026-09-21: **Fırat geri bildirimi (telefon kontrolü): ana gezinme yeniden düzenlendi.** İstek: "Akış'ın sol üstündeki asistan simgesine gerek yok; Profil'i oraya taşıyalım, ortaya arama koyalım (LinkedIn üst çubuğu görseliyle); Profil alt çubuktan kalkınca Hesaplamalar'ı alt çubuğa koyalım, içini sembollü butonlara çevirelim." Yapılan: ortak `MainHeader` (baş harfli profil düğmesi · "Arama Yap" · zil) beş sekmede; alt çubuk Akış · Ürünler · Asistan · Mesajlar · Hesap(lamalar); `MyProfile` kök yığına taşındı; Akış'ta gönderi paylaş yüzen "+" oldu; `GlobalSearchScreen` + `/api/search` (firma + kumaş + iplik, eşanlamlılarla, son aramalar, sonuç yoksa fotoğrafla ara); Hesaplamalar ekranı sembollü kart ızgarası (satırda 2). Tarayıcıda 375 px'te denendi. **Telefonda kontrol bekliyor.** **Açık:** kişisel profil fotoğrafı yok (başlıkta baş harfler); Asistan ekranındaki "Firma · İpek" alt başlığı yeni düzende kalktı; "İplik ihtiyacı" ikonu makarayı çağrıştırmıyor (MCI `spool` bu sürümde yok).

- 2026-09-21: **Fırat geri bildirimi: ürün formu sadeleşti, bakım bilgisi SEMBOLLE.** "İplik, sertifika, test raporu zaten giriliyor; en altta tekrar girmek gereksiz. Bakım bilgisi yazı değil sembol olsun." Yapılan: formdaki ayrı "AB pasaportuna hazırlık" bölümü kaldırıldı (menşe + geri dönüştürülmüş oran "Bilgiler"e taşındı; pasaport iplik/sertifika/test bilgisini girildiği yerden alıyor). Bakım: `backend/src/domain/care.ts` TEK KAYNAK (5 grup, 24 sembol, çizim tarifleriyle; grup başına en çok bir sembol), `Product.careSymbols` (elle yazılmış `ADD COLUMN` migration, şema farkı 0), `/api/dpp/care-symbols`, pasaport sayfasında SVG çizim, mobilde `CareSymbolIcon` + `CareSymbolPicker` (kopya liste: `mobile/src/features/care/symbols.ts`; değişirse ikisi birlikte güncellenir), ürün sayfasında "Bakım" bölümü. `careNotes` sütunu duruyor ama formdan artık girilmiyor. Test 20/20; tarayıcıda seç-kaydet-göster uçtan uca denendi. **Telefonda kontrol bekliyor** (sembollerin küçük ekranda okunurluğu). **Ders:** pasaport/rapor gibi TÜRETİLEN çıktılar için forma ayrı "yeniden gir" bölümü eklenmez; alan ait olduğu mevcut bölüme konur.

- 2026-09-21: **Faz 3 Adım 4, 5, 6 tamamlandı (Faz 3 bitti).** Sunucu (Fable 5.1): **Adım 4** (6ed45d9) `Deal` + `DealReview` (yalnızca yeni tablolar); kabul edilen teklif siparişe döner, anlaşılan tarih = kabul + satıcının kendi termini; teslim beyanı → alıcı onayı/itirazı, 7 gün kendiliğinden onay, iptal; değerlendirme karşılıklı açılır (ikisi de yazınca ya da 14 gün); alıcı→satıcı kalite/termin/iletişim, satıcı→alıcı iletişim/ciddiyet; ÖDEME ÖLÇÜTÜ YOK (test gövdede ödeme alanını reddettiriyor); test 32/32. **Adım 5** (3bf03d7) `/api/trust/company/:id`: tek puan yok, yalnızca bileşenler; eşik altı (3 iş, 3 değerlendirme, 5 istek) oran/ortalama gizli; yalnızca GÖRÜNÜR değerlendirmeler sayılır; teklife yanıt oranı + ortanca saat; test 11/11. **Adım 6** (a0fa07e) `/api/price-index/product/:id`: son 90 günde gönderilmiş teklifler, küme = çeşit+alt çeşit+ana lif+20 gr bandı (iplikte aile+numara ±%10), aynı para biriminde 5 satıcı + 8 teklif eşiği, yalnızca çeyrekler/ortanca, örnek sayısı kaba ("8+"), satıcıya kendi konumu; test 16/16. Mobil (Opus 5): `DealDetailScreen`, `DealsScreen` (Profil: Siparişlerim), teklif detayından/bildirimden geçiş, `TrustSummaryCard` (firma Hakkında sekmesi), `PriceIndexCard` (ürün sayfası, teklif detayı, karşılaştırma). Tarayıcıda sipariş akışı uçtan uca denendi. **Telefonda kontrol bekliyor.**

- 2026-09-21: **Faz 3 Adım 7 tamamlandı: AB Dijital Ürün Pasaportu'na HAZIRLIK.** Önce doğrulandı: resmi ESPR sayfasına göre tekstile özel zorunlu alanlar yayımlanmadı ("ürüne göre, istişareyle belirlenecek"); sektör kaynaklarına göre düzenleme 2027, uygulama 2028+. Bu yüzden hiçbir yerde "uyumlu" denmiyor. Sunucu (Fable 5.1, 0e7b3fc): `Product.originCountry/careNotes/recycledPercent` (Prisma tabloyu yeniden kurmak istedi; migration elle 3 `ADD COLUMN` olarak yazıldı, şema farkı 0 doğrulandı, canlıda sorunsuz), `/api/dpp/:id` (herkese açık belge; fiyat/stok/MOQ/termin ASLA çıkmaz; eksik listesi yalnızca sahibine), `/api/dpp/:id/qr.png` (`qrcode` paketi), `PUBLIC_WEB_URL` ile adres; test 14/14. Açık sayfa: `mobile/public/pasaport.html` (Vercel; TR/EN, JSON indirme, QR). Mobil (Opus 5): kumaş formunda "AB pasaportuna hazırlık" bölümü, ürün sayfasında "Dijital pasaport" (sayfayı aç, paylaş; sahibine % hazır + eksikler + QR). Tarayıcıda denendi. **Telefonda kontrol bekliyor.** **Açık:** iplik formunda bakım/geri dönüşüm alanı yok; kurallar yayımlanınca alan eşlemesi gözden geçirilecek.

- 2026-09-21: **Faz 3 Adım 3 tamamlandı: benzer kumaş arama.** Sunucu (Fable 5.1, cce2f31 + 8f6a916): `ProductLook` + `LookSearch` (yalnızca yeni tablolar), `skills/fabricLook` (görünüm kartı: desen, ölçek, renk, yüzey, doku, şeffaflık; gramaj/lif OKUNMAZ), deterministik ve nedenli benzerlik puanı (eşik 60; 45 iken canlıda zayıf eşleşmeler çıkıyordu), `/api/looks` (fotoğrafla arama günde 20, ürün sayfası benzerleri, sahibin yenilemesi), kapak değişince kart yenilenir, açılışta eksik kartlar doldurulur (canlıda 4 Melide ürünü kendiliğinden doldu; kartlar isabetli). Test 22/22 (sahte kip), gerçek modelle uçtan uca arama ~3 sn. Mobil (Opus 5): `SimilarSearchScreen` (Ürünler'de kamera düğmesi + Profil menüsü), ürün sayfasında "Benzer kumaşlar" şeridi, fotoğraf uzun kenardan küçültülür. Ayrıca küçük eksikler: ortak bildirim zili (Akış, Ürünler, Mesajlar, Profil), yalnızca arama metniyle izleme, izleme adını değiştirme. **Telefonda kontrol bekliyor** (kamera akışı yalnızca telefonda denenebilir). **Açık:** asistanda `benzer_kumas_ara` aracı yok. **SMS:** başvuru Melide İnşaat üzerinden AVEDON başlığıyla yapıldı, onay bekleniyor; uygulama adı Avedon kalıyor.

- 2026-09-18: **Faz 3 Adım 2 tamamlandı: asistanla teklif toplama.** Sunucu (Fable 5.1, 206e9da): `teklif_topla` (tüm firmalarda arar, firma başına tek aday, en çok 10; İSTEK GÖNDERMEZ, kart önerir; fiyat dönmez) ve `teklifleri_ozetle` (alıcının çoklu isteğine gelen teklifler, karşılaştırmalı). Gerçek modelle denendi: adayları buldu, "istek henüz gitmedi, karttan onaylayın" dedi, elastan farkını kendisi belirtti. Mobil (Opus 5): asistanda aday kartı (işaretle → "Seçilenlerden teklif iste" → form miktar/not ön dolu), karşılaştırma kartı, "Teklif topla" çipi, iplik dizininde seçme kipi, karşılaştırma tablosunda uzun metin için alt panel. Tarayıcıda uçtan uca denendi. **Telefonda kontrol bekliyor.**

- 2026-09-18: **Faz 3 planı onaylandı (`docs/faz3-plani.md`) ve Adım 1 tamamlandı: çoklu teklif isteme + karşılaştırma.** Kararlar (Fırat): sıra 1→2→3→7→4→5→6; kime gideceğini alıcı seçer, tek seferde en çok 10 FİRMA (5 az bulundu), firma başına tek istek, günde 30 istek; ödeme konusuna hiç girilmez; güven gösterimi yalnızca bileşenler; teklifler endekse kendiliğinden dahil; benzer kumaşta desen de var. Sunucu (Fable 5.1, c8a0dc0): `Rfq` + `QuoteRequest.rfqId` (yalnızca sütun + yeni tablo), `/api/rfqs`; satıcı çoklu olduğunu göremez; fiyat isteğin birimine çevrilir (kg↔m, gramaj+en), para birimi çevrilmez, en düşük fiyat işareti yalnızca aynı para biriminde; test 25/25. Mobil (Opus 5): Ürünler ve Takip Ettiklerim'de "Teklif için seç" kipi, `RfqFormScreen`, `RfqCompareScreen` (yatay kaydırmalı tablo), Tekliflerim'de "Karşılaştırmalar". Tarayıcıda uçtan uca denendi. **Telefonda kontrol bekliyor** (özellikle tablonun dar ekranda okunurluğu). **Açık:** iplik dizininde seçme kipi yok; tabloda uzun not/ödeme koşulu kırpılıyor. **SMS:** kod ve health teşhisi hazır (e87f2d8); başlık onayı şirket evrakı + KEP + e-Devlet BTK e-Kayıt ister (1-2 iş günü), adımlar `docs/sms-saglayici.md`.

- 2026-09-18: **Faz 2 açık küçük işler kapatıldı** (sunucu Fable 5.1 cd273f8; mobil Opus 5). (1) İplik izleme: `WatchRule.queryJson` içinde `kind: "iplik"`, iplik dizininde "Bu aramayı izle", İzlediklerim'de "İplik" etiketi (test 42/42). (2) Satıcı asistanı sohbeti 10 sn'de bir sessizce tazeleniyor. (3) Teklif gönderilince iki kişi BAĞLANTILIYSA sohbete kart olarak düşüyor (`Message.quoteRequestId`, yalnızca sütun eklendi; bağlantı yoksa yalnızca Tekliflerim + bildirim; test 35/35). (4) Yönetici ekranında doğrulama düzeyi (belge / ziyaret). (5) Eski `/products/:id/quote-request` ucu kaldırıldı. **Telefonda kontrol bekliyor.** Kalan: Adım 4 davet + SMS (en sonda), gerçek WhatsApp numarası.

- 2026-09-18: **Faz 2 Adım 6 tamamlandı: iplik dizini.** Kararlar Fırat'tan (bkz. `faz2-plani.md` karar 5). Sunucu (Fable 5.1, 3d0217b): iplik = `Product` (type `iplik`, stok kg) + yeni `YarnSpec` tablosu (yalnızca yeni tablo; mevcut tablolar yeniden kurulmadı) → teklif, numune, favori, fotoğraf, gönderi altyapısı iplikte de aynen çalışır; `/api/yarns` (seçenekler, arama, ekleme, düzenleme), numara birimden bağımsız aranır (`countDtex`: 150 denye = 167 dtex; tek değer ±%4), süzgeçler: aile, numara/aralık, filament, filament tipi, eğirme, penye/karde, kullanım yeri, renk durumu, lif, sertifika, stok, üretici/tüccar; kumaş kataloğu (`/api/products`) iplikleri döndürmez; asistan `iplik_ara`. Test 38/38, gerileme yok; canlıda doğrulandı. Mobil (Opus 5): Ürünler'de Kumaş | İplik ayrımı, iplik dizini, iplik formu, ürün detayında iplik görünümü, kumaş pasaportunda "Kim satıyor?", asistan kartı. **Telefonda kontrol bekliyor.** **Açık:** iplik için izleme kuralı ("şu iplik girilince haber ver") yok; etiketten iplik çıkarma yok. Kalan Faz 2: yalnızca Adım 4 davet (SMS ile birlikte, en sonda).

- 2026-09-18: **Faz 2 Adım 7 tamamlandı: doğrulama düzeyi + karşılıklı referans; kapasite aramasında sayfalama.** Sunucu (Fable 5.1, ace1e25): `CompanyReference` (A "B müşterimiz" = B "A tedarikçimiz", ayna kayıt tek sayılır; onaylanmamış referans yalnızca iki tarafa görünür; iki taraf da kaldırabilir), `/api/references`, `reference_*` bildirimleri, `Company.verificationLevel` (belge/ziyaret) + `verifiedAt` (admin doğrulama ucunda `level`; doğrulama geri alınınca temizlenir), `/api/machines/search` `offset` + `hasMore`/`nextOffset`. Company tablosu yeniden kuruldu; canlıda doğrulandı (Melide ürünleri yerinde). Test 26/26, gerileme yok. Mobil (Opus 5): firma sayfası Hakkında sekmesinde "Referanslar" (Müşterileri / Tedarikçileri, Onayınızı bekleyenler: Onayla/Reddet, gönderilen istekler: geri çek, Kaldır, başkasının sayfasında "Referans olarak ekle"), doğrulama satırı ("Belge ile doğrulandı · Eylül 2026", dokununca açıklama), bildirim yönlendirmesi, kapasite aramasında "Daha fazla göster". Tarayıcıda uçtan uca denendi. **Telefonda kontrol bekliyor.** **Açık:** admin panelinde düzey seçimi için ekran yok (şimdilik API ile); makine sırası değiştirme bilinçli olarak yapılmadı (Fırat: önemli değil, üretici giriş sırasını kendisi belirler). Kalan Faz 2: Adım 6 iplik dizini (Fırat alanları anlatacak), Adım 4 davet (SMS ile birlikte, en sonda).

- 2026-09-18: **Faz 2 Adım 5 tamamlandı: makine parkı (parkur) ve fason kapasite araması.** Kararlar (Fırat): makine TÜRÜ serbest metin, üretici yazar (öneriler başlangıç listesi + platformda girilenler), yalnızca geniş grup seçilir; kapasite = parkur + firmanın beyan ettiği aylık tonaj + fason açık işareti. Sunucu (Fable 5.1, 03b4832): `Machine`, Company kapasite alanları (Company tablosu yeniden kuruldu; canlıda doğrulandı, Melide ürünleri firma adlarıyla yerinde), `/api/machines` (öneriler, parkur, CRUD, kapasite beyanı, arama), asistan `kapasite_ara`; test 23/23, gerileme yok. Mobil (Opus 5): firma sayfasında 5. sekme "Makine parkı", `MachineParkScreen`, `MachineFormScreen`, `CapacitySearchScreen` (Profil: "Fason Kapasite Ara", "Makine Parkım"), asistanda "Fason kapasite" çipi ve kapasite kartı. **Telefonda kontrol bekliyor** (özellikle 5 sekmeli şerit). **Açık:** arama sayfalanmıyor; makine sırası değiştirilemiyor. Kalan Faz 2: Adım 4 davet (SMS ön koşul), Adım 6 iplik dizini (Fırat alanları bildirecek), Adım 7 doğrulama + referans.

- 2026-09-17: **Faz 2 Adım 3 tamamlandı: satıcı asistanı (alıcı sorularına katalogdan cevap; fiyat VERMEZ).** Sunucu (Fable 5.1, 5cfa9e7): `buyer` kanalı + `targetCompanyId`, `CompanyFaq`, `CompanyQuestion`; `assistant/buyer.ts` ayrı talimat ve araç seti (fiyatsız `katalog_ara`, `sss_oku`, `soruyu_ilet`, açık hesap becerileri; firma hafızası ve sahibin araçları yok), günlük 40 soru sınırı; uçlar seller thread, SSS CRUD, iletilen sorular + cevap (alıcının ipliğine ve bildirimine düşer, SSS'ye eklenebilir). Test 33/33 (fiyat/hafıza/başka firma sızıntı kontrolleri); gerçek modelle sızıntı denemesi temiz (fiyat, kâr oranı, sistem talimatı reddedildi; özel renk sorusu firmaya iletildi). Mobil (Opus 5): `SellerAssistantScreen` (firma ve ürün sayfasında "Asistana sor"), `CompanyQuestionsScreen` (Firmam'da "Asistana gelen sorular (N)"), `CompanyFaqScreen`, ortak `components/assistant/ChatParts`, bildirim yönlendirmeleri. **Telefonda kontrol bekliyor.** **Açık:** cevap gelince açık sohbet kendiliğinden tazelenmiyor. Sıradaki: Adım 4 davet mekaniği (SMS ön koşul → önce Adım 5 makine parkı).

- 2026-09-17: **Faz 2 Adım 2 tamamlandı: teklif akışı.** Sunucu (Fable 5.1, c5823d9): `QuoteRequest`, `Quote`, `/api/quotes` (istek, taslak, düzenle, gönder, kabul/ret, geri çek, süre dolumu), beceri `quoteDraft`, `quote_*` bildirimleri; bağlantı şartı yok, fiyat uydurulmaz, fiyat yalnızca taraflara; test 33/33. Mobil (Opus 5): `QuoteRequestFormScreen`, `QuoteRequestDetailScreen` (alıcı: teklif kartı + Kabul/Reddet; satıcı: form + "Ürün kaydından doldur" + Teklifi gönder), `QuoteRequestsScreen` (Verdiğim / Gelen), Profil'de "Tekliflerim", Firmam'da "Gelen teklif istekleri (N)", akış kartı ve ürün sayfasında "Teklif iste" artık forma gider (eski sohbetli akış kaldırıldı). Ürün sayfası alt çubuğundan "Firma" düğmesi kalktı (firma satırı üstte duruyor). **Telefonda kontrol bekliyor.** **Açık:** teklif kartı sohbete düşmüyor; sunucuda eski `/products/:id/quote-request` ucu temizlenecek. Sıradaki: Adım 3 satıcı asistanı (fiyat vermez).

- 2026-09-17: **Faz 2 planı onaylandı (`docs/faz2-plani.md`, Fırat'ın 6 kararıyla) ve Adım 1 tamamlandı: bildirimler + akış izleme.** Sunucu (Fable 5.1, b3bc204): `Notification`, `WatchRule`, `WatchMatch`; olaylar: izleme eşleşmesi (yeni ürün), numune talebi ve durum değişimi, bağlantı isteği; `/api/notifications`, `/api/watch-rules`; asistan `izleme_oner` / `izlemeleri_listele`; test 26/26, gerileme yok. Mobil (Opus 5): Akış başlığında sayaçlı zil, `NotificationsScreen`, `WatchRulesScreen` (İzlediklerim; Profil menüsü), süzgeç ekranında izleme kipi, Ürünler'de "Bu aramayı izle", asistanda izleme öneri kartı ve "Ürün izle" çipi. **Telefonda kontrol bekliyor.** **Açık:** push bildirimi yok (60 sn yoklama); zil yalnızca Akış'ta; yalnızca arama metni varken "Bu aramayı izle" çıkmıyor; kural adı ekrandan değiştirilemiyor. Sıradaki: Adım 2 teklif akışı (bağlantı şartı yok; satıcı asistanı fiyat vermez).

- 2026-09-17: **Aşama B 4. parça tamamlandı: adım adım firma sayfası ("Firma sayfanı tamamla")** ve açık işlerden üçü. (1) `CompanySetupScreen`: 5 adım (Tanıtım, İletişim ve adres, Logo, Fotoğraflar, Ekip ve ilk ürün), ilerleme çubuğu, Atla / Kaydet ve devam, her adım kendi alanlarını PATCH eder; Firmam'da yüzde şeridi + "Tamamla" (`features/companies/completeness.ts`); ortak parçalar `CompanyLogoPicker`, `useCompanyGalleries`, `validation.ts`; tek form (Firmayı Düzenle) duruyor. Tarayıcıda doğrulandı (şerit %0, 1/5 Tanıtım adımı). (2) Ürün sayfasında `PassportCard`, test raporlarının formdan girilmesi (1e9df9e). (3) Enin anlamı: `Product.widthMeaning` (acik | tup_tek_yuz), `effectiveWidthCm`, formda "Girdiğiniz en neyi gösteriyor?" (3f668c1, fefd61c; pasaport API 71/71). (4) Konfeksiyon hesaplayıcısı ayrı kalemler. **Telefonda kontrol bekliyor:** firma kurulum akışı, test raporu formu, tüp en seçimi. **Açık:** kurulumda "Atla" girilmiş ama kaydedilmemiş değişiklikleri uyarısız atıyor; e-posta biçimi istemcide denetlenmiyor.

- 2026-09-17: **TELEFON KONTROLÜ GEÇTİ (Fırat):** Etiketten doldur (kamerayla gerçek etiket), Asistan sekmesi (İpek/Mert seçimi, hesap sorusu, hafıza kartında Kaydet), Akış (pasaport kartı, Takibe al, Bağlantılarım), konfeksiyon maliyeti kalemleri ve WhatsApp asistanı telefonda "hepsi düzgün çalışıyor". Faz 1 Adım 3, 5, 6, 7, 9 için "telefonda denenmedi" notları kapandı. Konfeksiyon hesaplayıcısı ayrı kalemlere geçti (3c99ac1, 261e16d; 315 ₺ örneği tarayıcıda doğrulandı).

- 2026-09-17: **WhatsApp → asistan CANLIDA ÇALIŞIYOR** (Meta test numarası +1 555 159 1243; Fırat kendi telefonundan sordu, asistan cevap verdi; health: imza hatası 0, `wabaSubscription: ok`). Kurulumda yaşananlar `docs/whatsapp-kurulum.md` "Sorun giderme" bölümüne işlendi: (1) App secret yanlış kopyalanmıştı → imza 403 (health `signatureFailureCount` ile görüldü); (2) Render'a geçici token girilmişti, süresi doldu → sistem kullanıcısından süresiz token; (3) uygulama Unpublished'dı → gizlilik sayfası `mobile/public/gizlilik.html` (avedon-blond.vercel.app/gizlilik.html) eklenip yayınlandı; (4) asıl neden: mesajlar yanlış numaraya yazılıyordu. Yeni teşhisler: health `postCount`, `lastPostSummary`, `signatureFailureCount`, `wabaSubscription`; yeni env `WHATSAPP_WABA_ID` (açılışta abonelik). **Açık:** test numarası yalnızca listeye eklenen 5 telefona cevap verir; gerçek numara için ayrı SIM + Step 2/3 (işletme doğrulaması); numune bildirimi şablonu (`WHATSAPP_TEMPLATE_NAME`) yok.

- 2026-09-16: **Adım 9 tamamlandı: asistan kişiliği İpek / Mert ve çizim avatar** (kullanıcı kararı; yol haritası §5). Sunucu (Fable 5.1, fd993a7): `User.assistantPersona`, `assistant/persona.ts` (karakter sesi sistem talimatına), `GET/PUT /api/assistant/persona`, modelsiz karşılama `GET /api/assistant/greeting` (saat, ad, bekleyen numune talebi, okunmamış mesaj, boş hafıza ipucu); gerçek modelle Mert doğrudan, İpek sıcak tonda tanıttı. Mobil (Opus 5, bende tarayıcıda doğrulandı): `react-native-svg` ile `AssistantAvatar` (İpek/Mert düz geometrik büst; idle/listening/thinking/speaking/result halleri; hareketi azalt desteği), ilk açılışta "Kim yardımcı olsun?" seçim kartları (`PersonaPicker`), boş sohbette büyük avatar + karşılama balonu, son asistan mesajında canlı avatar, Firma hafızası ekranında "Asistan · Değiştir". Ses ve video yok (karar). **Telefonda denenmedi**; avatar çizimi ilk sürüm, geri bildirime göre oran/hat düzeltilebilir; karşılama sohbet sürerken yenilenmiyor.

- 2026-09-16: **Faz 1, Adım 7 kod tarafı tamamlandı: WhatsApp → asistan** (Fable 5.1). Webhook imza doğrulaması (`WHATSAPP_APP_SECRET`), tekrar koruması (`WhatsAppInbound.messageId`), gönderen eşleme (90… → 0…), kanal `whatsapp` asistan ipliği, cevap = metin + araç özetleri + hafıza notu; bilinmeyen numaraya kayıt yönlendirmesi; `WHATSAPP_MOCK` sahte kip; health `whatsapp` alanı. Testler 117/117 birim, webhook 22/22; gerileme yok. **Canlıda henüz çalışmıyor:** Meta WhatsApp Business hesabı/anahtarları gerekiyor (Fırat); hazır olunca panel adımları resmi dokümandan doğrulanıp tek seferde verilecek, doğrulama `/api/health` `whatsapp.lastWebhookVerifiedAt`/`lastInboundAt` ile. **Açık:** WhatsApp'tan ürün taslağı (Faz 2); `WHATSAPP_APP_SECRET` canlıda boşsa imza doğrulanmıyor (health'te görünür, canlıya alırken zorunlu).

- 2026-09-16: **Faz 1, Adım 6 tamamlandı: akışta pasaport kartları ve ticari eylemler** (sunucu fc07201, Fable 5.1; mobil Opus 5 alt görevi, bende tarayıcıda doğrulandı). Ürünlü gönderi artık **kumaş pasaportu kartı** (`PassportCard`: kod, KUMAŞ PASAPORTU, gramaj/en/çeşit, kompozisyon, stok noktası + MOQ/termin, sertifika rozetleri; fiyat akışta yok). Eylemler: **Numune talep et**, **Takibe al / Takipte** (= mevcut ProductFavorite; Favorilerim → **Takip Ettiklerim**, ürün sayfasında yıldız → yer imi), **Teklif iste** (bağlantılı kişiyle sohbet açar, ürün kodlu hazır mesaj; bağlantı yoksa bağlantı isteği önerir; kendi ürününde gizli). Ürünlü gönderide beğeni yok (karar: beğeni → Takibe Al), yorum kalır; ürünsüz duyurularda beğeni/yorum/paylaş aynen. Akışta **Tümü / Bağlantılarım** sekmeleri (`scope=connections`: bağlantılı kişiler + firmalarındaki herkes; seçim cihazda hatırlanır). Yeni ürün kaydından sonra "Akışta paylaşılsın mı?" sorusu (otomatik gönderi yok). Testler: `scripts/test-feed-passport.ts` 28/28, gerileme yok. **Telefonda denenmedi**; Teklif iste'nin bağlantılı yolu (sohbet açılışı) yalnızca API'de test edildi, ekranda denenmedi; "Akışta paylaş" akışı ekranda denenmedi. **Açık:** `PassportCard` ürün sayfasında ve asistan sonuçlarında da kullanılabilir; "Numune talep et" kendi ürününde de görünüyor.

- 2026-09-16: **Faz 1, Adım 5 sunucu tarafı: firma asistanı** (mobil ekran Opus alt görevinde). Yeni tablolar `AssistantThread`, `AssistantMessage` (ekran içeriği + ham API mesajları), `CompanyMemory`; migration `20260916180000_assistant_threads_memory` yerel kopyada prova edildi. `backend/src/assistant/`: sistem talimatı (arkadaş tonu, "hesabı sen yapmazsın", Fırat'ın 5 kuralı), araçlar (11 beceri + `katalog_ara` firmanın kendi ürünleri + `pasaport_cikar` + `firma_hafizasi_oku` + `hafiza_oner`), SDK araç döngüsü (`client.beta.messages.toolRunner`, en fazla 8 tur, sabit talimat önbellekli), sahte kip. Uçlar `/api/assistant/threads*`, `/api/assistant/memory*`; `/api/health` `assistant.configured`. Testler: asistan API 36/36 (sahte), gerçek modelle 7 tur: iplik çevirisi, metre-kilo + hafıza önerisi, katalog (fiyatı boş ürünlerde rakam uydurmadı), üretim (hafıza boşken sordu, verilince hesapladı + 3 kayıt önerisi), tüp en sorusu (tek yüz mü açık mı diye sordu, cevaba göre 160 cm ile hesapladı). **Fırat'ın 6 cevabı** `docs/faz1-plani.md` Adım 4'e işlendi ve becerilere uygulandı (yeni `yarnRequirement`, `garmentCost` kalemleri, `widthMeaning`, `machineCount`, gramaj A/B/C). **Mobil (Opus 5 alt görevi, tarayıcıda doğrulandı):** alt menüde Hesaplamalar sekmesi **Asistan** oldu (kök boya kızılı yalnızca asistanda), `AssistantScreen` (sohbet, `AssistantResultCard` kesik çizgili sonuç kartı, hafıza kartı Kaydet/Şimdi değil, beceri çipleri, `Tüm hesaplayıcılar` → yığındaki `CalculatorsList`), `AssistantThreadsScreen` (sohbet listesi, yeni/sil), `AssistantMemoryScreen` (firma hafızası düzenleme); son threadId cihazda, sohbet sunucuda; eski `AdvisorScreen` ve `askAdvisor` kaldırıldı. Tarayıcı testi: gerçek modelle "250 kg kaç metre, boya firemiz %6 kaydet" → cevap + metre-kilo kartı + hafıza kartı → Kaydet → Firma Hafızası ekranında "Boya firesi 6" ve veritabanında kayıt. **Telefonda denenmedi.** **Açık:** üst banttaki firma adı ağır `fetchCompany` ile geliyor (`/api/me` firma adı dönerse hafifler); hafıza kartının Kaydedildi durumu sohbet yeniden yüklenince kaybolur; yanıt akışı (stream) yok; taslaktaki "Teklife dönüştür" düğmeleri yok; mobil `GarmentCostCalculator` ekranı hâlâ 4 kalem (kesim/dikim/yıkama-baskı/aksesuar/paketleme/nakliye/genel gider ayrımına geçmeli); ürün pasaportuna "enin anlamı" ve hesap eni ile A/B/C gramaj alanları; eski `/api/advisor/ask` bir sürüm daha duruyor; WhatsApp kanalı (Adım 7).

- 2026-09-16: **Faz 1, Adım 4 tamamlandı: hesap motorları backend'de, beceri iskeleti** (`docs/faz1-plani.md` Adım 4 + uygulama notu). Formüller mobilden birebir kopya (`backend/src/domain/calc/formulas.ts`), `npm run check:formulas` eşitliği doğrular; mobil hesaplayıcılar değişmedi. Beceri sözleşmesi (`skills/types.ts`, zod/v4) + 10 beceri (`skills/calc/*`) + `GET/POST /api/skills` (iskelet ve örnek beceri Fable 5.1; kalan beceriler, testler, scriptler Opus 5 alt görevi; hepsi bende doğrulandı: tsc temiz, birim 100/100, API 38/38, kopya eşitliği). Görünür değişiklik yok; Adım 5 asistanı bu becerileri araç yapacak. **Fırat'a sorular** (açıklama metinleri için, hesabı etkilemez): randıman/saat varsayılanı, tüp ende metretül, konfeksiyon kalemleri, iplik ihtiyacı firesinin türü, ham gramaj sapması, iplik satır adları, her hesap için 2-3 gerçek örnek.

- 2026-09-16: **Faz 1, Adım 3 tamamlandı: "Etiketten doldur"** (`docs/faz1-plani.md` Adım 3 + uygulama notu; sunucu commit e64667c, mobil bir sonraki commit). **Sunucu (Fable 5.1):** `POST /api/passport/extract` fotoğraf (≤4) / PDF / metinden pasaport önerisi; yapılandırılmış çıktı (`messages.parse` + `zodOutputFormat`, claude-opus-5). İç içe şema API gramer sınırını aştı → model her alanı METİN olarak yazdığı gibi aktarır, `finalize.ts` Adım 1 ayrıştırıcılarıyla anahtara/sayıya çevirir, kanıtla doğrular (≥0,95), makullükle güveni düşürür, eşik 0,4 altını `rejected`'a atar. Fiyat/stok/MOQ/termin şemada yok. `ANTHROPIC_MOCK=1` sahte kip; ortak istemci `backend/src/llm.ts` (advisor ve garmentAnalysis de buna geçti). Testler: birim 38/38, uçtan uca `scripts/test-passport-extract-api.ts` 36/36, gerçek model çizilmiş etikette 11/11 alan doğru (8 sn, ~3,3k giriş jetonu), dağınık metinde fiyat/MOQ aktarılmadı. Fırat'ın makullük aralıkları `plausibility.ts`'e işlendi. **Mobil (Opus 5 alt görevi):** ürün formunda "Etiketten doldur" (Fotoğraf çek / Galeriden seç / PDF seç / Metin yapıştır; `expo-document-picker` eklendi), `PassportReviewScreen` (onay kutulu satırlar, güven rozeti, kanıt, aktarılmayanlar, notlar, uyarı), "Forma aktar" → form dolar (çeşit, alt çeşit, kod, kompozisyon satırları, gramaj, en, en tipi, iplik, sertifika, apre, kullanım), kayıtta `fieldMeta` extracted+confirmed. **Düzeltme:** alt görev `navigate`+merge kullanmıştı, React Navigation 7'de bu formun ikinci kopyasını açıyor → `popTo`; aynı hata SelectProduct → CreatePost'ta da vardı, düzeltildi. **Tarayıcıda doğrulandı** (gerçek model, metin girişi): 11 alan okundu, tek form ekranına geri döndü, kaydedildi, sunucuda kompozisyon/iplik/sertifika/fieldMeta (11 satır, onaylı) yazıldı. **Telefonda denenmedi** (kamera, galeri çoklu seçim, PDF seçimi el testi ister). **Açık:** etiket fotoğrafı ürün galerisine eklenmiyor (planda "istenirse"); düzenleme kipinde eski `fieldMeta` satırı elle değişiklikte sunucuda silinmiyor (silme ucu yok); gerçek kartela fotoğraflarıyla 10 örnek doğruluk notu bekliyor (Fırat'tan fotoğraf).

- 2026-09-16: **Faz 1, Adım 1 + Adım 2 tamamlandı ve canlıda** (`docs/faz1-plani.md`; commit'ler bc7aa79, 9fa7d0b, 76c464c). **Adım 1 (sözlük, Fable 5.1):** `backend/src/domain/glossary/` — lif/örgü/sertifika eşanlamlıları, Türkçe katlama, açık kısaltma listesi (CO, EA, PES...; "yün", "kot", "GRS" gerçek ad), iplik numarası çevrimleri (mobil formüllerle aynı), gramaj/en/metretül, birimli metin ayrıştırma, kompozisyon ayrıştırma/biçimleme (35 gerçek yazım), makullük aralıkları (BAŞLANGIÇ değerleri, Fırat doğrulayacak). Arama artık eşanlamlıları biliyor ("single jersey" → süprem). Birim testleri 20/20 (`npm test`, node --test + tsx). **Adım 2 sunucu (Fable 5.1):** Product'a widthType, moq/moqUnit (stok biriminden bağımsız), leadTimeDays, fiyat (yalnızca sahibe döner, başkasına alan hiç yazılmaz), finishTags, passportUpdatedAt; yeni tablolar ProductComposition, ProductYarn, ProductCertificate, ProductTestReport, ProductFieldMeta. Migration `20260916120000_fabric_passport` yerel kopyada prova edildi (Prisma SQLite'ta tabloyu yeniden kurdu, 14 sütunun hepsi kopyalandı, veri kaybı yok) ve **canlıda uygulandı** (Render 76c464c, Melide'nin 3 ürünü yerinde, yanıtlarda yeni alanlar). Kompozisyon verilirse içerik metni ondan üretilir; yalnızca metin verilirse ayrıştırılır ve güven 0,9+ ise `parsed_content` kaynağıyla yazılır (onay bekler). Kayıt engellenmez: toplam ve makullük uyarı olarak döner. Detayda iplik/sertifika/test raporu; belge fotoğrafları ayrı uçtan; alan onayı `POST /products/:id/fields/confirm`; admin `POST /admin/products/backfill-composition?dryRun=1`. Yeni filtreler fiber/fiberMinPercent/certificate/moqMax/leadTimeMax/widthType. Testler: pasaport API 66/66, katalog API 98/98 (gerileme yok). **Adım 2 mobil (Opus 5 alt görevi):** ürün formunda kompozisyon satırları, Ticari bölümü, en tipi, apre etiketleri, kapalı gelen İplik/Sertifikalar (`CollapsibleSection`), sunucu uyarısı için sarı kutu + "Devam"; ürün sayfasında kompozisyon çubukları, ticari bilgiler, fiyat (sahibe), sertifika/iplik/test raporu, onay şeridi + Onayla; liste satırında kompozisyon metni + sertifika rozeti; filtre ekranında lif (+en az %), sertifika, MOQ, termin, en tipi. `tsc` temiz. **Tarayıcıda doğrulandı:** form (bölümler, İplik/Sertifika kapalı, toplam %98 notu, makul olmayan gramajla kayıt → "Kaydedildi. Dikkat: Gramaj..." + Devam; sunucuya kompozisyon/MOQ/termin/fiyat doğru yazıldı), ürün sayfası (tüm bölümler), onay şeridi (Onayla → sunucuda bekleyen 0), filtre ekranı (Elastan filtresi listeyi süzdü, çip kaldırma). Vercel yeni paket yayında. **Telefonda denenmedi.** **Açık:** (1) canlıdaki 3 Melide ürününün kompozisyonu boş; ya Fırat ürünü Düzenle → Kaydet ile yeniden kaydeder (metin ayrıştırılır) ya da admin girişiyle `backfill-composition` çalıştırılır (Render paneli oturumu kapanmış, kod okunamadı). (2) Test raporları formdan girilmiyor (yalnızca gösteriliyor). (3) ~~Fırat'tan listeler~~ → Fırat 2026-09-16: eşanlamlı/iplik/apre listelerini asistan süreç içinde kendisi yönetsin (gerçek etiketlerden öğrenilen sözlüğe işlenir); makullük: kumaş 60-400 gr/m², tül/raschel kaliteleri 25-200 gr/m², en 90-200 cm (çoğunluk 145-165) → `plausibility.ts`'e işlendi. Sıradaki: Adım 3 (etiketten doldur).

- 2026-09-16: **Aşama B, 3. parça — firma galerileri (ofis fotoğrafları ve sertifikalar).** Yeni `CompanyPhoto` tablosu (kind: office | certificate, sıralar 0..n-1, galeri başına en fazla 12 fotoğraf); migration `20260916100000_company_photos` yerel kopyada prova edildi. `GET /api/companies/:id` yalnızca `officePhotoCount` / `certificatePhotoCount` döndürüyor, fotoğrafın kendisi `GET /api/companies/:id/photos/:kind/:position` ile tek tek geliyor (ürün galerisindeki kural). `PATCH /api/companies/:id` artık `officePhotos` / `certificatePhotos` alıyor: liste o galerinin tam hali, mevcut fotoğraflar `{ existing: sıra }` ile yeniden yüklenmeden korunuyor. Firma sayfasının Hakkında sekmesinde iki yatay galeri (dokununca tam ekran), Firmayı Düzenle ekranında iki fotoğraf ızgarası (ekle, kaldır, başa al) — yeni ortak bileşenler `CompanyPhotoGallery` ve `PhotoGridEditor`. Test 98/98 (yeni: kaydetme, tek tek okuma, yeniden sıralama, olmayan sıra 400 ve hiçbir şeyin değişmemesi, iki galerinin bağımsızlığı, 13 fotoğraf 400, boş listeyle temizleme, fotoğrafın firma yanıtına sızmaması). Tarayıcıda doğrulandı: Hakkında sekmesinde galeriler ve tam ekran, düzenleme ekranında ızgaralar. **Telefonda denenmedi.** Kalan: (4) adım adım şirket sayfası oluşturma.

- 2026-09-16: **Aşama B, 2. parça — firma bilgileri genişledi.** Company modeline yeni alanlar: `companyType` (katalogdaki COMPANY_TYPES: kumaş üreticisi, konfeksiyon, boyahane, iplik, aksesuar, baskı, toptancı, diğer), `foundedYear`, `website`, `city`, `district`, `address`, `mainMarkets`. Migration `20260916090000_company_profile_fields` yerel kopyada prova edildi (5 firma, ürünler ve gönderiler eksiksiz). Firma sayfasının Hakkında sekmesinde artık "Şirket genel bakışı" bölümü var: şirket tipi, kuruluş yılı, ürün grupları (firmanın ürünlerinden hesaplanıyor, ayrı alan değil), şehir/ilçe, ana pazarlar; İletişim bölümünde web sitesi (dokununca tarayıcıda açılır) ve adres. Firmayı Düzenle formuna yedi alan eklendi; kuruluş yılı 1800-bu yıl arası dört hane değilse Kaydet pasif. Test 83/83 (yeni: alanların kaydı ve geri dönüşü, bilinmeyen şirket tipi 400, gelecekteki yıl 400, null ile temizleme, başka firmayı düzenleyememe). Tarayıcıda doğrulandı: form kaydı ve Hakkında sekmesinde görünüm. **Telefonda denenmedi.** **Not:** Windows'ta sunucu çalışırken `prisma generate` dosya kilidi yüzünden sessizce eskimiş istemci bırakabiliyor; şema değişince sunucuyu durdurup üretmek gerekiyor (testler bu yüzden bir tur kırmızı verdi).

- 2026-09-16: **Aşama B, 1. parça — firma sayfası sekmelere ayrıldı.** Orijinal tasarımdaki dört sekme: **Hakkında · Ürünler · Firma Akışı · Kişiler**. Hakkında: tanıtım yazısı (boşsa kendi firmana yönlendiren metin), iletişim (e-posta, telefon, VKN, kendi firmanda şirket kodu) ve özet (ürün/kişi sayısı, doğrulama durumu). Ürünler: çeşit çipleri (Tümü + firmada bulunan çeşitler, sayılarıyla) ve kullanım amacı çipleri ile süzme. Akış: `GET /api/posts?companyId=` (yeni süzgeç; görünürlük kuralları aynen geçerli) ile firmanın gönderileri, akıştaki kartın aynısı — beğeni, yorum, paylaş, kendi gönderinde düzenle/sil. Kişiler: firmadaki kullanıcılar, kendin "(siz)" etiketiyle. Test 77/77 (yeni: firma akışı yalnızca o firmanın gönderilerini verir, başka firmanınkinde çıkmaz). Tarayıcıda doğrulandı: dört sekme, çeşit çipiyle süzme, akışta gönderi, kişiler listesi. **Telefonda denenmedi.** Sonraki parçalar: (2) firma bilgileri genişliyor (adres, şehir, kuruluş yılı, şirket tipi, ana pazarlar, web sitesi) ve düzenleme formu; (3) ofis ve sertifika fotoğrafları galerisi; (4) adım adım şirket sayfası oluşturma.

- 2026-09-16: **Ürün seçici ölçeklendi + akışta ürün fotoğrafı.** Kullanıcı: "firmaların yüzlerce kumaş numunesi olacak, bu durum sayfada çok fazla yer kaplar" ve "ürün resmi akışta görünmüyor". (1) Gönderi ekranında ürünler artık listelenmiyor: tek satır — seçiliyse ürünün küçük resmi + kodu + çeşidi ile "Değiştir/Kaldır", değilse "Ürün Seç". Seçim ayrı ekranda (SelectProduct): arama SUNUCUDA, tek seferde ilk 30 ürün, "N üründen ilk M" notu; seçim merge ile geri dönüyor, yazılan metin ve eklenen fotoğraf korunuyor. (2) `GET /api/products/mine` artık `search` + `limit` (en çok 100) alıyor ve `total` döndürüyor; sıralama en yeni ürün üstte. (3) Akış kartı: gönderide kendi fotoğrafı/videosu yoksa ürünün kapak fotoğrafı gösteriliyor, dokununca ürün sayfası açılıyor (`POST_PRODUCT_SELECT` ürünün fotoğrafı var mı bilgisini taşıyor). Test 74/74 (yeni: seçici araması/limiti/toplamı, akışta ürün fotoğrafı bilgisi ve fotoğrafın yanıtta sızmaması). Tarayıcıda doğrulandı: akışta ürün fotoğrafı 317x178 çizildi. **Telefonda denenmedi.**

- 2026-09-15: **Kullanıcı: "Gönderi paylaşırken ürün ekleyemiyorum" — düzeltildi.** Teşhis (canlıda ölçüldü): seçici çalışıyordu (Fırat DEMİR / Melide, /api/products/mine 3 ürün döndürüyordu, çip seçiliyordu) ama sayfanın en altında, yalnızca ürün koduyla duruyordu; ürün sayfasından paylaşma yolu da yoktu. Yapılan: (1) ürün bölümü yazı alanının hemen altına taşındı, ürünler küçük resim + kod + çeşit satırları, seçili satır onay işaretli; (2) seçilen ürünün kapak fotoğrafı "Ürün fotoğrafını gönderiye ekle" ile gönderiye eklenir; (3) firmanın ürünü yoksa açıklama + "Ürün Ekle" düğmesi; (4) kendi ürününün sayfasında "Paylaş" düğmesi gönderi ekranını o ürün seçili açar; (5) /api/products/mine alt çeşit ve fotoğraf bilgisini de döndürür. Test 64/64 (seçici için 3 yeni kontrol). Tarayıcıda yerelde doğrulandı: boş durum, satır seçimi, ürün fotoğrafını ekleme, gerçek paylaşım (gönderi ürün + fotoğrafla kaydedildi, sonra silindi), ürün sayfasından Paylaş ile ön seçili açılış. **Telefonda denenmedi.**

- 2026-09-15: **Aşama A — Katalog derinliği** (plan: `docs/orijinal-proje-plani.md`). Kullanıcı kararları aynı gün: A ile başla · ilk demoda numune ödemesini firmalar kendi yöntemiyle halleder · abonelik/ücret sonraki aşamalarda · görüntülü görüşme yok. **Yapılanlar:** (1) Kumaş kataloğu: 6 çeşit (Örme, Raschel, Dokuma, Dantel, Triko, Diğer) → alt çeşitler + 14 kullanım amacı (çoklu); anahtarlar `backend/src/catalog.ts`, uygulamadaki kopya `mobile/src/features/products/catalog.ts` (`npx tsx scripts/check-catalog.ts` eşitliği denetler). (2) Stok birimi metre/kg (az stok eşiği 100 m / 30 kg). (3) Çoklu ürün fotoğrafı (en fazla 6, ilki kapak): yeni `ProductImage` tablosu; güncellemede mevcut fotoğraflar sırasıyla gönderilir, yeniden yüklenmez; eski uygulamaların `imageUrl` alanı çalışmaya devam eder. (4) Filtreler: çeşit, alt çeşit, kullanım amacı, stok birimi/en az stok, gramaj ve en aralığı, içerik; arama çeşit/alt çeşit/kullanım etiketlerinde ve firma adında da arar. (5) Favoriler ve Son Baktıklarım (sunucuda, her cihazda aynı; son bakılan kullanıcı başına 50, kendi firmanın ürünleri tutulmaz). (6) Ürünler ekranı: filtre düğmesi + kaldırılabilir filtre çipleri, Çeşitler görünümünde kullanım amacı kısayolları, klasör içinde alt çeşit çipleri, klasörde kapak fotoğrafı. (7) Ürün sayfası: kaydırmalı galeri, favori yıldızı, alt çeşit/kullanım/not satırları. (8) Ürün formu Pazar Masası düzenine geçti (fotoğraf ızgarası, çip seçimleri, sabit kaydet çubuğu, silme onayı `confirmAction`). (9) Orijinal Avedon logosu giriş ekranında (`mobile/assets/brand/`). **Migration** `20260915180000_catalog_gallery_favorites`: yerel veritabanı kopyasında prova edildi (eski fotoğraf galeriye kapak olarak geçti, numune/gönderi/firma verisi eksiksiz). **Test:** `node --env-file=.env --import tsx scripts/test-catalog-api.ts` → 61/61 (fotoğraf sıralama/silme, hatalı alt çeşit, tüm filtreler, favori/son bakılan kuralları, 50 sınırı, silmede temizlik, eski sürüm uyumu). Tarayıcıda doğrulandı: klasörler, alt çeşit çipleri, kullanım kısayolu, filtre ekranı (hatalı aralık uyarısı, uygulama), ürün sayfası ve favori kaydı. **Canlıda doğrulandı (651812b):** Render yeni sürümde, migration gerçek veride çalıştı; Melide'nin 2 ürününün fotoğrafı galeriye kapak olarak geçti (/image ve /images/0 aynı, 200), geçersiz filtre 400; Vercel yeni paketi sunuyor. Canlı ürünlerde alt çeşit/kullanım amacı boş: firma ürünü düzenleyip seçmeli. Giriş logosu tarayıcıda 280x45 ölçüldü. **Telefonda denenmedi.** Bilinen sınır: klasör sayıları o an yüklü listeden hesaplanıyor (katalog büyüyünce sunucuda sayfalama + sayım gerekecek).

- 2026-09-15: **Orijinal Avedon tasarımı görsellerle incelendi; fark analizi ve aşama planı hazır — kullanıcı onayı bekliyor.** Kullanıcının Drive klasörü "Avedon Tasarım"dan (onayıyla) 84 ekran (Kasım 2020), 95 ekran (26 Mayıs 2021 = v4.2 XD) ve logo dosyaları indirildi → `docs/orijinal-tasarim/` (repoya girmez). Plan: **`docs/orijinal-proje-plani.md`** (A katalog derinliği · B firma sayfası · C numune ve güven · D sosyal · E akıllı arama/takvim · F abonelik). Görsel dil C · Pazar Masası olarak kalır; orijinalden logo, kategori kareleri, firma sekmeleri alınır.

- 2026-09-15: **Kullanıcı geri bildirimi (telefon + web kontrolü) — 4 madde düzeltildi (bb38725).** (1) **56 sn video web'de yüklenmiyordu:** `expo-file-system`'in web sürümü boş (yükleme görevi hiçbir şey yapmıyor); önceki başarılı yüklemeler telefon uygulamasındandı. Web'de artık FormData + XMLHttpRequest. **Canlıda kanıtlandı:** Vercel sayfasından yükleme adresi 201, tarayıcıdan Cloudflare'e ilerleme dinleyicili yükleme 200 (CORS sorunsuz, anahtarın yazma izni var), video `processing`e geçti, test videosu silindi (204). (2) **Yazı küçük:** ölçek ~2px büyüdü (gövde 14→16), ekranlardaki sabit boyutlar da. (3) **Ürün klasörleri:** Ürünler'de "Tümü / Çeşitler" seçimi; Çeşitler'de Raschel/Örme/Dokuma/Diğer klasörleri, seçim cihazda hatırlanır (yerelde doğrulandı). (4) **Profil avatarında logo yoktu:** `GET /api/users/:id` `logoUpdatedAt` göndermiyordu, `ProfileIdentity` de avatara firma bilgisini vermiyordu; canlı yanıtta alan geliyor. Kullanıcı ayrıca beğendiği tasarımın avedon-blond.vercel.app'teki (C · Pazar Masası) olduğunu, tasarımın buna göre sürmesini istedi; orijinal projenin görsel çalışmalarını yeniden paylaşmayı önerdi.

- 2026-09-15: **Web'de oturum yenilemede düşüyordu — düzeltildi.** Oturum jetonu `expo-secure-store` ile saklanıyordu; bu paketin web uygulaması yok (SDK 57 dokümanı: Android/iOS/tvOS), web'de her açılışta `getValueWithKeyAsync is not a function` hatası çıkıyor, yazma hatası da sessizce yutuluyordu. Sonuç: canlı web sürümünde (Vercel) sayfa her yenilendiğinde yeniden giriş gerekiyordu. `features/tokenStorage.ts`: telefonda şifreli depo aynen, web'de AsyncStorage (localStorage). Tarayıcıda doğrulandı: giriş → jeton depoda → yenile → Akış'ta kalındı, hata yok. Bilinen ödünleşim: web'de jeton localStorage'da; kalıcı çözüm httpOnly çerez.

- 2026-09-13: **Görsel dil geçişi** yapıldı. Öncesinde güncel (2026) mobil tasarım pratikleri araştırıldı (bkz. aşağıdaki kaynaklar) ve 2020 tasarımı bugünün ölçütleriyle karşılaştırıldı — çoğu kararı hâlâ geçerli çıktı. **Alınanlar:** hap (tam yuvarlak) butonlar ve form alanları — Material 3'ün 2025 güncellemesinde de varsayılan bu oldu; akış kartında fotoğraf→metin sırası; ikonlu, eşit sütunlu 4'lü aksiyon satırı (Beğen · Yorum Yap · Paylaş · Talep Et); açık mavi tonal yüzeyler. **Uyarlananlar:** kalın mavi fotoğraf çerçevesi yerine yumuşak köşe + hafif gölge (çerçeveli fotoğraf bugün eskimiş duruyor). **Alınmayan:** italik başlıklar — Türkçe'de ğ/ş/ı/ç italikte okunurluğu düşürüyor, kimlik lacivert + kalın ile kuruluyor. **Tasarımda olmayan, eklenenler:** tek bir yazı ölçeği (`typography`, öncesinde ekranlara dağılmış 10–22 arası rastgele boyutlar vardı), 48px minimum dokunma hedefi (Apple 44 / Google 48), ortak `Badge` bileşeni (aynı rozet sekiz ekranda kopyalanmıştı), başlık çubuklarının tek elden biçimlendirilmesi. 39 dosya tek ölçeğe taşındı. **Cihazda test edilmedi.** Kaynaklar: [Material 3 Expressive](https://www.designwhine.com/material-3-expressive-details/) · [2026 mobil UI trendleri](https://muz.li/blog/whats-changing-in-mobile-app-design-ui-patterns-that-matter-in-2026/) · [thumb zone](https://timgraf.com/ux-design/designing-for-the-thumb-zone-a-modern-guide-to-mobile-ux-that-respects-human-anatomy/) · [B2B pazaryeri özellikleri](https://www.rigbyjs.com/blog/b2b-marketplace-features)
- 2026-09-13: **Ürün detay sayfası** eklendi. Ürün kartına (listede ve firma sayfasında) dokunmak artık ürün sayfasını açıyor: tam genişlik fotoğraf — dokununca tam ekran büyüyor —, firma kartı + "Doğrulanmış Üretici" ve "Toplam N Ürün" rozetleri, özellikler kartı (kod, tip, stok, ağırlık, genişlik, içerik, kullanım alanı), numune talebi / ürünü düzenleme ve firma sayfası düğmeleri. Fotoğraflar artık liste ve detay yanıtlarında **dönmüyor**; `GET /api/products/:id/image` ile tek tek çekilip önbelleğe alınıyor — tek bir ürünün fotoğrafı 201 KB olduğu için katalog büyüdükçe her liste isteği (ve tüm kataloğu tek yanıtta veren firma sayfası) megabaytlara çıkardı. Gönderilerdeki aynı düzeltmenin önbellek mantığı ortak bir modüle alındı. Ürün uçlarındaki yakalanmayan async hataları da düzeltildi. 30 otomatik uçtan uca kontrol geçti. **Cihazda test edilmedi.**
- 2026-09-13: **Numune takip zaman çizelgesi** tamamlandı (yol haritası 7. madde). Yeni `SampleRequestEvent` modeli her adımın kim tarafından ne zaman yapıldığını saklıyor (sadece eklenir; migration mevcut talepler için ilk adımı geriye dönük doldurdu). Bir adımın tamamlanmış sayılması **statüden** türetiliyor, olay kaydının varlığından değil — geçmişi eksik eski kayıtlar listedeki rozetle çelişmesin diye. Teslimat tercihi serbest metinden iki seçeneğe indi (satıcı gönderir / müşteri kuryesi alır) ve son adımın **etiketi** buna göre değişiyor, statü tek kalıyor. Tüm etiket ve açıklama metinleri sunucudan geliyor (istemcide ikinci bir kopya tutulmuyor). Bu arada dört mevcut kusur düzeldi: async hataları yakalanmıyordu (istek asılı kalıyordu), yanıtlarda telefon numarası ve base64 ürün fotoğrafı sızıyordu, statü merdiveni adım atlamaya/aynı adımı tekrarlamaya izin veriyordu, yetkisiz erişim 403 dönüp talep id'sinin varlığını doğruluyordu. Mobilde: dikey zaman çizelgesi ekranı (dolu/boş daireler, tarih-saat, adımı yapan kişi ve firması, teslim notu), talep formunda iki seçenekli teslimat kartları, her iki talep listesi de karta dokununca takibe gidiyor. 40 otomatik uçtan uca kontrol geçti. **Cihazda test edilmedi.**
- 2026-09-12: Backend + mobile ilk kurulum (npm install, `.env`, Prisma migrate+seed, Expo çalıştırma)
- 2026-09-12: Telefon–bilgisayar bağlantı sorunu çözüldü (AVG Antivirus firewall'u engelliyordu)
- 2026-09-12: Anthropic API anahtarı eklendi, AI Danışman + Kıyafet Analizi aktif edildi
- 2026-09-12: Git kimliği ayarlandı, otomatik commit/push izni kuruldu (`CLAUDE.md`, `.claude/settings.json`)
- 2026-09-13: **Gerçek kimlik doğrulama** eklendi — SMS OTP tabanlı kayıt/giriş, 30 günlük oturum JWT'si, admin/ürün/numune talebi sahiplik kontrolleri artık sunucuda (client'a güvenmiyor). Eski `/api/login` ve "her kod kabul edilir" davranışı kaldırıldı.
- 2026-09-13: **2020 vizyonu incelendi** (`Takyon Ai - Avedon Product.pdf`) ve kullanıcı tam sosyal ağ + B2B hibriti vizyonunu onayladı; aşamalı yol haritası oluşturuldu (yukarıda).
- 2026-09-13: **Navigasyon yeniden yapılandırıldı ve cihazda test edildi.** Akış artık ana ekran; altta 4 sekme (Akış / Ürünler / Mesajlar / Profil). Keşfet ve takvim henüz olmadığı için 5 değil 4 sekme — çalışmayan sekme koymaktansa eksik bırakıldı. Ana ekrandaki 10 düğmelik sıra dağıtıldı: kumaş araçları (AI Danışman, Hesap Araçları, Görsel Maliyet, Firmam) Ürünler sekmesinde, ikincil hedefler yeni Profil sekmesindeki menüde. Giriş durumuna göre iki ayrı ekran grubu render ediliyor — giriş/kayıt/çıkış sonrası gezinme çağrısı yok; bu, kayıt biten kullanıcının geri tuşuyla kayıt adımlarına dönebilmesi hatasını da düzeltti. Sekmedeki profil rotası bilinçli olarak `MyProfile` adını taşıyor: `Profile` iki param listesinde birden olsaydı akışta birinin adına dokununca kendi profilin açılırdı. Cihazda doğrulandı (geri tuşu, çift başlık yok, başkasının profili doğru açılıyor, sekme geçişinde liste korunuyor, klavyede sekme çubuğu gizleniyor, mesaj rozeti, çıkış).
- 2026-09-13: **Tasarım paleti uygulandı** — tasarım dosyasından piksel düzeyinde okunan mavi aile (`#133C5F` lacivert, `#2696C6` mavi vurgu) koyu yeşilin yerine geçti.
- 2026-09-13: **Faz 1–3 gerçek cihazda test edildi ve geçti.** Akış (gönderi görme/beğenme/yorum/paylaşma/"Talep Et"), bağlantı kurma (istek → kabul → telefon numarasının görünür olması → gizli gönderinin akışta belirmesi) ve mesajlaşma (canlı gelen mesaj + okundu bilgisi 5 sn içinde) telefonda doğrulandı. Türkçe karakterler cihazda doğru görünüyor — test sırasında görülen `?` işaretleri yalnızca asistanın git-bash üzerinden curl ile gönderdiği mesajlarda oluşan kodlama bozulmasıydı, uygulamayla ilgisi yok (veritabanında byte düzeyinde doğrulandı). **Not:** Admin ile olan test sohbetinde bu bozuk iki mesaj duruyor; istenirse silinebilir.
- 2026-09-13: **Faz 3 — İçerik akışı (feed)** tamamlandı. `Post` / `PostLike` / `PostComment` modelleri, `/api/posts` (akış, oluştur, sil, beğen/geri al, yorum ekle/listele/sil, fotoğraf). Görünürlük: `public` herkese, `connections` sadece yazarın bağlantılarına — ve bu kural sadece listede değil yorum/beğeni/fotoğraf uçlarında da uygulanıyor. Gönderiye sadece KENDİ firmasının ürünü iliştirilebiliyor (başkasının ürünüyle talep toplanmasın diye), ürün silinirse gönderi ayakta kalıp bağlantı boşalıyor. Fotoğraflar liste yanıtında dönmüyor (bir sayfa megabaytlara çıkıyordu); `hasImage` ile işaretlenip kart görünürken tek tek çekiliyor ve önbelleğe alınıyor. Mobilde: akış ekranı (sonsuz kaydırma, aşağı çekip yenileme, iyimser beğeni), gönderi oluşturma (fotoğraf + görünürlük + ürün seçici), yorumlar ekranı, firma baş harfi + doğrulama rozeti. 46 otomatik uçtan uca kontrol geçti (görünürlük sızıntısı, yan yollardan erişim, ürün iliştirme kuralı, imleçli sayfalama, `http://` izleme pikseli reddi).
- 2026-09-13: **Faz 2 — Mesajlaşma** tamamlandı. `Conversation` + `Message` modelleri (userAId < userBId kanonik sıralama), `/api/conversations` (başlat/listele/mesajlar/gönder/okundu/okunmamış-sayısı). Sohbet başlatmak ve yazmak için kabul edilmiş bağlantı şart, okumak için değil. Gerçek zamanlılık polling ile (sohbet 4sn, liste 15sn; uygulama arka plandayken duruyor). Mobilde: Mesajlar listesi (okunmamış rozeti, istemci tarafı arama), sohbet ekranı (iyimser gönderim + hata durumunda tekrar dene), yeni sohbet için bağlantı seçici, profilde "Mesaj Gönder", ana ekranda okunmamış sayaçlı "Mesajlar" chip'i. Uçtan uca curl ile doğrulandı (yabancı sohbete erişim 403, bağlantısız kişiye mesaj 403, boş mesaj 400, bozuk `since` 400, Türkçe karakterler byte-birebir).
- 2026-09-13: **Faz 1 — Bağlantı sistemi + Profil sayfası** tamamlandı. Yeni `Connection` modeli (pending/accepted, reddetme = satır silme), `POST/PATCH /api/connections`, `GET /api/connections(?status=)`, `GET /api/connections/status/:userId`, `GET /api/users/:id` (bağlantı yoksa telefon alanı gizli). Mobilde: Profil ekranı (bağlantı kur/kabul/reddet butonları), Bağlantılarım ve Bağlantı İstekleri ekranları, firma sayfasında tıklanabilir çalışan listesi, gelen numune taleplerinde talep edenin adı artık profile bağlıyor. Uçtan uca curl ile doğrulandı (istek/kabul/red/kendine-istek-engeli/tekrar-istek-engeli/telefon gizliliği). Plan: `C:\Users\ebosc\.claude\plans\swift-rolling-kazoo.md`.
