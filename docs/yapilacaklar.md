# Açık İşler / Yapılacaklar

Bu dosya, sohbet geçmişinde kaybolmaması gereken önemli notları ve bekleyen kararları tutar. Yeni bir çalışma oturumuna başlarken önce burası kontrol edilir.

## Açık İşler

- **VİDEO — 1. AŞAMA (AKIŞ GÖNDERİLERİ) KODLANDI, CLOUDFLARE HESABI BEKLENİYOR (2026-09-14):** Sunucu: `Video` modeli + migration `add_videos`, `src/stream.ts` (Cloudflare istemcisi; uç yolları ve `result.token` API referansından doğrulandı), `src/videos.ts` (durum tazeleme, erişim kontrolü, silme), `routes/videos.ts` (`POST /upload-url`, `GET /:id`, `GET /:id/playback`, `DELETE /:id`), gönderiye `videoId` bağlama ve gönderi silinince Stream'den silme. Sahte Cloudflare ile 38 uçtan uca kontrol geçti (`DELETE /:id` ucu bu testten sonra eklendi, henüz testte yok). Mobil: `expo-video` + `expo-file-system` kuruldu (Expo Go'da çalışır), `features/videoUpload.ts` (galeriden seçim, 60 sn kontrolü, doğrudan Cloudflare'e yükleme + ilerleme), `components/PostVideo.tsx` (işleniyor/hata/küçük resim + oynat), gönderi ekranında "Video Ekle". `.env`'de `CLOUDFLARE_ACCOUNT_ID` ve `CLOUDFLARE_STREAM_API_TOKEN` boş yer tutucu olarak duruyor; boşken video düğmesi "henüz etkinleştirilmedi" diyor. **Sıradaki:** kullanıcı anahtarları girince gerçek hesapla telefonda deneme; sonra 2. aşama Ürün sayfası, 3. aşama Sohbet. **Canlıya çıkmadan önce:** Render'da da iki anahtarın girilmesi ve istenirse webhook eklenmesi. **Bilinen açık:** paylaşılmadan uygulama kapatılırsa yüklenen video sahipsiz kalabilir (ekrandan normal çıkışta siliniyor); periyodik temizlik gerekebilir.
- **VİDEO — KARARLAR VERİLDİ (2026-09-14):** servis **Cloudflare Stream** (1.000 dk depo aylık 5$ ön ödemeli, izlenen 1.000 dk 1$, kodlama ücretsiz, ücretsiz plan yok); süre sınırı **60 saniye**; kapsam **Akış gönderileri + Ürün sayfası + Sohbet mesajları** (üçü de). Bağlantılara özel gönderiler ve sohbet videoları herkese açık bağlantıyla izlenememeli, yani imzalı oynatma gerekiyor. **Kullanıcının yapacağı:** Cloudflare hesabı açmak, Stream'i etkinleştirip depo satın almak ve API anahtarı üretmek (ödeme ve hesap işlemleri asistan tarafından yapılamaz). İlk kayıt:
- **(ilk kayıt) GÖNDERİYE VİDEO EKLEME — KULLANICI İSTEDİ (2026-09-14):** "Akış'taki ekle butonu sadece resim bekliyor, video ilavesi yapılabilir mi; bazı anlatımlar ve görseller önemli." Teknik durum: `expo-image-picker` (`mediaTypes: ['images','videos']`, sonuçta `duration`/`fileSize`/`mimeType`) ve oynatma için `expo-video` SDK 57'de **Expo Go'ya dahil** — yeniden derleme gerekmiyor. Engel saklama: bugünkü base64-in-SQLite yöntemi (`express.json` 15 MB sınırı, fotoğraf ~200 KB) video için uygun değil; Android'de cihaz üstü video sıkıştırma Expo Go'da yok (`videoExportPreset`/`videoQuality` yalnızca iOS). **Nesne depolama servisi gerekiyor ve hesabı kullanıcı açmalı** (asistan hesap açamaz). Seçenekler: Cloudflare Stream (sıkıştırma + uyarlanır oynatma, ücretli), Cloudflare R2 (ham dosya deposu), Supabase Storage. Kararlar: servis, video süre sınırı, videonun yalnızca gönderilerde mi ürün sayfasında da mı olacağı. Not: fotoğraflar da aynı depoya taşınırsa veritabanı şişmesi sorunu da kökten çözülür. Ayrıca repoda Render dağıtım ayarı yok; canlı ortamın SQLite dosyasını kalıcı diskte tutup tutmadığı doğrulanmadı.

- **YENİDEN TASARIM — YÖN SEÇİLDİ: C · PAZAR MASASI (2026-09-14).** Kullanıcı "3 model daha güzel" dedi. Sıradaki adım: C yönünü ana ekranların tamamına taslak olarak genişletmek (Ürünler, Mesajlar, Sohbet, Profil, Numune takibi, Firma, Hesaplamalar, Giriş) ve onay almak; onaydan sonra `MOBILE-DESIGN.md` yazılıp aşamalı uygulamaya geçilecek. Not: C'de butonlar hap değil 6px köşeli; bu, bir önceki turda onaylanan hap butonların yerini alıyor (kullanıcı C'yi bu hâliyle gördü ve seçti). Seçim öncesi kayıt:
- **(eski kayıt) YENİDEN TASARIM — KULLANICININ YÖN SEÇMESİ BEKLENİYOR (2026-09-14):** Kullanıcı "baştan modern bir yeniden tasarım" istedi (önce görsel taslak + onay, sonra kod). Denetim yapıldı: `mobile/design-baseline.json` (17 bulgu, kritik yok; en zayıf alan yükleme/boş/yenileme durumları, ikinci büyük sorun her ekranın "gri zemin + beyaz kutu" olması). Üç yön aynı iki ekranda (Akış + Ürün sayfası) çizildi ve tuvale konuldu: https://claude.ai/code/artifact/2a3759b3-2620-4d87-8ccb-c35dd01bbdae — **A Rafine Lacivert** (mevcut lacivert/mavi, Figtree, kutu yerine ince çizgi), **B Ham Kumaş** (ham bez zemin, kök boya vurgu, Newsreader + Instrument Sans, kumaş etiketi imza öğesi), **C Pazar Masası** (yoğun B2B, IBM Plex Sans + Mono, ölçüler akışta). Taslak kaynakları `docs/tasarim-yonleri/`. **Seçim gelince:** seçilen yön tüm ekranlara taslak olarak genişletilir, onaylanınca sırayla uygulanır (1 yazı/boşluk, 2 renk + koyu mod, 3 durumlar, 4 basma geri bildirimi, 5 ekran düzenleri). Navigasyon değişmiyor, denetim gerek görmedi.

- ~~Telefonda test edilmeyi bekliyor~~ → **2026-09-13 tarihinde gerçek cihazda test edildi ve geçti** (aşağıda Tamamlananlar'a bakın).
- ~~Navigasyon yeniden yapılandırması~~ → **2026-09-13'te yapıldı ve cihazda test edildi** (Tamamlananlar'a bakın).
- **Google ile giriş — KARAR VERİLDİ, ön hazırlık kullanıcıda:** Kullanıcı Google ile girişin büyük kolaylık sağladığını belirtti ve tasarımda da var (`2.1 Login Screen`: Google / Facebook / LinkedIn). **Karar:** telefon + SMS akışı kalacak, Google ile giriş **üzerine eklenecek**; Google'la giren yeni kullanıcıdan bir kez telefon doğrulaması istenecek. Gerekçe: kimlik modeli telefon merkezli (WhatsApp bildirimleri, "bağlantı kurunca telefon görünür" kuralı, `User.phone` unique). **Başlamadan önce kullanıcının Google Cloud Console'da bir OAuth istemcisi oluşturup client ID'yi vermesi gerekiyor** — asistan hesap açamaz/kimlik bilgisi giremez.
- **Tasarımdan çıkan, henüz uygulanmamış farklar** (bkz. `docs/tasarim-envanteri.md`): (1) Tasarımda mesajlaşma bir sekme değil, **üst barda rozetli ikon**; 5. sekme **Takvim**. Bizde Mesajlar sekme (keşfet/takvim olmadığı için bilinçli). (2) Butonlar/input'lar tasarımda **tam yuvarlak (hap)**, bizde `radius.md = 12`. (3) Üst barda arama kutusu + logo yok. (4) Tasarım dosyasındaki 129 ekranın görseli henüz dışa aktarılmadı (`Dosya → Dışa Aktar → Tüm Artboard'lar`).
- **Güvenlik/olgunluk boşlukları (bilinçli olarak ertelendi):** (1) Uygulamada hiçbir yerde hız sınırlama (rate limiting) yok — akış, bir kullanıcının yazdığının herkese yayıldığı ilk özellik olduğu için bu eksiklik ilk kez burada anlam kazanıyor. (2) Şikayet/engelleme mekanizması yok; tek moderasyon imkânı gönderi sahibinin kendi gönderisindeki yorumu silebilmesi. (3) `GET /api/connections` tam `User` satırı dönüyor, telefon numarası dahil — bağlantılar arasında savunulabilir ama daraltılmalı. (4) Beğeni/yorum bildirimi yok; doğal olarak sekme rozetleriyle birlikte, navigasyon yeniden yapılandırmasında ele alınmalı. (5) `Company.logoUrl` alanı yok — akış kartlarında firma logosu yerine baş harf gösteriliyor.
- ~~Tasarım dosyaları incelenmeyi bekliyor~~ → **ikisi de incelendi.** PDF (9 ekran) ve XD dosyası (132 ekran) çözümlendi; bulgular `docs/tasarim-envanteri.md`, ham ekran metinleri `docs/xd-ekran-metinleri.md`. **`.xd` dosyası bir ZIP arşivi** — Adobe XD olmadan `unzip` + `docs/xd-extract.js` ile okunabiliyor.
- **Telefon–bilgisayar bağlantısı kalıcı olarak çözülmedi (2026-09-14):** "Something went wrong" yine çıktı; telefon Metro'ya hiç ulaşamadı (kod hatası değil). İki engel tespit edildi: (1) ev Wi-Fi'si (`FiberHGW_ZT57YQ`) Windows'ta **"Genel ağ"** olarak kayıtlı ve Genel profilde `C:\program files\nodejs\node.exe`'yi **engelleyen iki güvenlik duvarı kuralı** var — Windows'ta engelleme kuralı izin kuralını geçersiz kıldığı için eklediğimiz "Avedon Backend / Avedon Expo Metro" izinleri işlemiyor; (2) AVG güvenlik duvarı (`afwServ`) açık. AVG geçici kapatılınca çalıştı, ama ağ profili hâlâ Genel. **Kalıcı çözüm kullanıcıda:** Ayarlar → Wi-Fi → ağ özellikleri → "Özel ağ" + AVG'de ev ağını güvenilir işaretlemek. Asistan güvenlik ayarlarını kendisi değiştirmez.
- **Gerçek SMS sağlayıcısı henüz yok:** OTP kodları şu an sadece backend konsoluna yazdırılıyor (geliştirme modu). Yayına çıkmadan önce Twilio gibi bir sağlayıcı eklenmeli (`backend/src/sms.ts` içinde yorum olarak nasıl ekleneceği yazılı).

## Sonraki Özellik Kararı

Gerçek kimlik doğrulama tamamlandı (bkz. Tamamlananlar).

**2020 vizyonu (Takyon Ai PDF) — KARAR VERİLDİ (2026-09-13):** PDF, Avedon'u feed/hikayeler/keşfet/mesajlaşma/bağlantı-network sistemi olan tam bir sosyal medya platformu olarak tanımlıyor (bkz. `Takyon Ai - Avedon Product.pdf`, 9 ekran). Kullanıcı **"Evet"** diyerek bu tam vizyonu (sosyal ağ + B2B ticaret hibriti) hedef olarak onayladı — seçici/kısmi yaklaşım değil, tam kapsam.

**Aşamalı yol haritası (bağımlılık sırasına göre):**
1. **Bağlantı/Network sistemi + zenginleştirilmiş profil sayfası** — PDF'e göre mesajlaşma ve birçok UI durumu ("bağlantı kur" vs "mesaj gönder") buna bağlı, bu yüzden temel/ilk aşama.
2. **Mesajlaşma** — bağlantı sistemine bağımlı.
3. **İçerik akışı (feed)** — gönderi paylaşma, beğeni, yorum.
4. **Hikayeler** (24 saatlik içerik).
5. **Keşfet / haber akışı** (otomatik içerik toplama, kişiselleştirme).
6. **Kategori kataloğu** genişletmesi (üretici/konfeksiyoncu/boyahane vb. alt kategoriler).
7. **Numune sürecini zaman damgalı timeline'a dönüştürme.**
8. **Firma sayfasını zenginleştirme** (çalışan listesi, sertifikalar, ofis görselleri).

**1., 2. ve 3. aşama + navigasyon yeniden yapılandırması tamamlandı ve cihazda test edildi.**

**XD tasarımı incelendikten sonra güncellenen öncelik önerisi** (bkz. `docs/tasarim-envanteri.md` sonuç bölümü) — sıradaki adaylar, etki/maliyet sırasına göre:
1. ~~**Numune takibini zaman damgalı timeline'a çevirmek**~~ → **2026-09-13'te yapıldı** (Tamamlananlar'a bakın). **Cihazda test edilmeyi bekliyor.**
2. ~~**Ürün detay sayfası**~~ → **2026-09-13'te yapıldı** (Tamamlananlar'a bakın). Tasarımdan **bilinçli olarak ertelenen** parçaları: (a) **favori/yıldız** — `Favorite` modeli gerektiriyor, ayrı iş; (b) **ürün bazında görünürlük** ("Herkese Açık / Sadece Size Açık") — şu an tüm katalog herkese açık, `GET /api/products` kimlik doğrulaması bile istemiyor; (c) alt çubuktaki **telefon ve mesaj ikonları** — telefon numarası gizlilik modelimizde ancak bağlantı kurulunca görünüyor, mesajlaşma da kabul edilmiş bağlantı istiyor; ikisi de çoğu ziyaretçide çalışmayan düğme olurdu, yerine "Firma Sayfasını Aç" kondu; (d) fotoğrafta **parmakla yakınlaştırma** — `react-native-gesture-handler` bağımlılığı gerektiriyor, tam ekran görüntüleme şimdilik yeterli.
3. ~~**Görsel dil geçişi**~~ → **2026-09-13'te yapıldı** (Tamamlananlar'a bakın).
4. **Ürün sayfasına MOQ (minimum sipariş miktarı) + termin süresi** — kullanıcı kararı (2026-09-13): **MOQ ve termin görünsün, fiyat görünmesin**; tekstilde fiyat pazarlıkla belirleniyor. B2B araştırmasında da alıcının ilk sorduğu iki bilgi bunlar. `Product` modeline iki alan (`moqMeters`, `leadTimeDays`) + ürün formu + özellikler kartı satırı gerekiyor.
5. **Kategori ağacı** (6. madde): taksonomi tasarımdan hazır çıktı (30+ örme tipi, sektör ayrımı, kullanım amacı). Ürün arama/filtrelemeyi gerçekten kullanılır yapar.
6. **Firma sayfası alanları** (8. madde): kuruluş yılı, firma tipi, çalışan sayısı, ana pazarlar, sertifikalar, ofis görselleri.
7. **Koyu mod** — 2026 araştırmasında "opsiyonel özellik" sayılmıyor; tasarımda da bizde de yok. Tema dosyası merkezî olduğu için maliyeti orta ama her ekranın iki hâlinin gözle test edilmesi gerekiyor. Kullanıcı görsel dil turunun dışında tutmayı seçti (2026-09-13).
8. **Google ile giriş** (yukarıdaki açık iş; kullanıcının Google Cloud hazırlığı gerekiyor).
9. Hikayeler / Takvim — vizyonun sosyal katmanı. (Keşfet kullanıcı kararıyla yol haritasından çıkarıldı, yerini Hesaplamalar sekmesi aldı. Hikayeler görsel dil turunda **ertelendi**: B2B araştırması keşiften çok hızlı sipariş ve bilgi netliğinin kazandığını söylüyor.)
10. **Abonelik/ödeme** — iş modeli kararı gerektiriyor (3 ay ücretsiz + aylık abonelik + ilk 10 ürün ücretsiz).

Diğer, vizyonla ilgisiz adaylar (bkz. `docs/durum.md`):
- Ödeme/fatura akışı (hiç yok)
- WhatsApp webhook'unu platform içi mesajlaşmaya bağlama
- Mobile `Company` tipi ile Prisma şeması arasındaki `productCategories`/`employeeIds` tutarsızlığını gidermek

## Tamamlananlar (kısa özet)

- 2026-09-13: **Görsel dil geçişi** yapıldı. Öncesinde güncel (2026) mobil tasarım pratikleri araştırıldı (bkz. aşağıdaki kaynaklar) ve 2020 tasarımı bugünün ölçütleriyle karşılaştırıldı — çoğu kararı hâlâ geçerli çıktı. **Alınanlar:** hap (tam yuvarlak) butonlar ve form alanları — Material 3'ün 2025 güncellemesinde de varsayılan bu oldu; akış kartında fotoğraf→metin sırası; ikonlu, eşit sütunlu 4'lü aksiyon satırı (Beğen · Yorum Yap · Paylaş · Talep Et); açık mavi tonal yüzeyler. **Uyarlananlar:** kalın mavi fotoğraf çerçevesi yerine yumuşak köşe + hafif gölge (çerçeveli fotoğraf bugün eskimiş duruyor). **Alınmayan:** italik başlıklar — Türkçe'de ğ/ş/ı/ç italikte okunurluğu düşürüyor, kimlik lacivert + kalın ile kuruluyor. **Tasarımda olmayan, eklenenler:** tek bir yazı ölçeği (`typography`, öncesinde ekranlara dağılmış 10–22 arası rastgele boyutlar vardı), 48px minimum dokunma hedefi (Apple 44 / Google 48), ortak `Badge` bileşeni (aynı rozet sekiz ekranda kopyalanmıştı), başlık çubuklarının tek elden biçimlendirilmesi. 39 dosya tek ölçeğe taşındı. **Cihazda test edilmedi.** Kaynaklar: [Material 3 Expressive](https://www.designwhine.com/material-3-expressive-details/) · [2026 mobil UI trendleri](https://muz.li/blog/whats-changing-in-mobile-app-design-ui-patterns-that-matter-in-2026/) · [thumb zone](https://timgraf.com/ux-design/designing-for-the-thumb-zone-a-modern-guide-to-mobile-ux-that-respects-human-anatomy/) · [B2B pazaryeri özellikleri](https://www.rigbyjs.com/blog/b2b-marketplace-features)
- 2026-09-13: **Ürün detay sayfası** eklendi. Ürün kartına (listede ve firma sayfasında) dokunmak artık ürün sayfasını açıyor: tam genişlik fotoğraf — dokununca tam ekran büyüyor —, firma kartı + "Doğrulanmış Üretici" ve "Toplam N Ürün" rozetleri, özellikler kartı (kod, tip, stok, ağırlık, genişlik, içerik, kullanım alanı), numune talebi / ürünü düzenleme ve firma sayfası düğmeleri. Fotoğraflar artık liste ve detay yanıtlarında **dönmüyor**; `GET /api/products/:id/image` ile tek tek çekilip önbelleğe alınıyor — tek bir ürünün fotoğrafı 201 KB olduğu için katalog büyüdükçe her liste isteği (ve tüm kataloğu tek yanıtta veren firma sayfası) megabaytlara çıkardı. Gönderilerdeki aynı düzeltmenin önbellek mantığı ortak bir modüle alındı. Ürün uçlarındaki yakalanmayan async hataları da düzeltildi. 30 otomatik uçtan uca kontrol geçti. **Cihazda test edilmedi.**
- 2026-09-13: **Numune takip zaman çizelgesi** tamamlandı (yol haritası 7. madde). Yeni `SampleRequestEvent` modeli her adımın kim tarafından ne zaman yapıldığını saklıyor (sadece eklenir; migration mevcut talepler için ilk adımı geriye dönük doldurdu). Bir adımın tamamlanmış sayılması **statüden** türetiliyor, olay kaydının varlığından değil — geçmişi eksik eski kayıtlar listedeki rozetle çelişmesin diye. Teslimat tercihi serbest metinden iki seçeneğe indi (satıcı gönderir / müşteri kuryesi alır) ve son adımın **etiketi** buna göre değişiyor, statü tek kalıyor. Tüm etiket ve açıklama metinleri sunucudan geliyor (istemcide ikinci bir kopya tutulmuyor). Bu arada dört mevcut kusur düzeldi: async hataları yakalanmıyordu (istek asılı kalıyordu), yanıtlarda telefon numarası ve base64 ürün fotoğrafı sızıyordu, statü merdiveni adım atlamaya/aynı adımı tekrarlamaya izin veriyordu, yetkisiz erişim 403 dönüp talep id'sinin varlığını doğruluyordu. Mobilde: dikey zaman çizelgesi ekranı (dolu/boş daireler, tarih-saat, adımı yapan kişi ve firması, teslim notu), talep formunda iki seçenekli teslimat kartları, her iki talep listesi de karta dokununca takibe gidiyor. 40 otomatik uçtan uca kontrol geçti. **Cihazda test edilmedi.**
- 2026-09-12: Backend + mobile ilk kurulum (npm install, `.env`, Prisma migrate+seed, Expo çalıştırma)
- 2026-09-12: Telefon–bilgisayar bağlantı sorunu çözüldü (AVG Antivirus firewall'u engelliyordu)
- 2026-09-12: Anthropic API anahtarı eklendi, AI Danışman + Kıyafet Analizi aktif edildi
- 2026-09-12: Git kimliği ayarlandı, otomatik commit/push izni kuruldu (`CLAUDE.md`, `.claude/settings.json`)
- 2026-09-13: **Gerçek kimlik doğrulama** eklendi — SMS OTP tabanlı kayıt/giriş, 30 günlük oturum JWT'si, admin/ürün/numune talebi sahiplik kontrolleri artık sunucuda (client'a güvenmiyor). Eski `/api/login` ve "her kod kabul edilir" davranışı kaldırıldı.
- 2026-09-13: **2020 vizyonu incelendi** (`Takyon Ai - Avedon Product.pdf`) ve kullanıcı tam sosyal ağ + B2B hibriti vizyonunu onayladı; aşamalı yol haritası oluşturuldu (yukarıda).
- 2026-09-13: **Navigasyon yeniden yapılandırıldı ve cihazda test edildi.** Akış artık ana ekran; altta 4 sekme (Akış / Ürünler / Mesajlar / Profil). Keşfet ve takvim henüz olmadığı için 5 değil 4 sekme — çalışmayan sekme koymaktansa eksik bırakıldı. Ana ekrandaki 10 düğmelik sıra dağıtıldı: kumaş araçları (AI Danışman, Hesap Araçları, Görsel Maliyet, Firmam) Ürünler sekmesinde, ikincil hedefler yeni Profil sekmesindeki menüde. Giriş durumuna göre iki ayrı ekran grubu render ediliyor — giriş/kayıt/çıkış sonrası gezinme çağrısı yok; bu, kayıt biten kullanıcının geri tuşuyla kayıt adımlarına dönebilmesi hatasını da düzeltti. Sekmedeki profil rotası bilinçli olarak `MyProfile` adını taşıyor: `Profile` iki param listesinde birden olsaydı akışta birinin adına dokununca kendi profilin açılırdı. Cihazda doğrulandı (geri tuşu, çift başlık yok, başkasının profili doğru açılıyor, sekme geçişinde liste korunuyor, klavyede sekme çubuğu gizleniyor, mesaj rozeti, çıkış).
- 2026-09-13: **Tasarım paleti uygulandı** — tasarım dosyasından piksel düzeyinde okunan mavi aile (`#133C5F` lacivert, `#2696C6` mavi vurgu) koyu yeşilin yerine geçti.
- 2026-09-13: **Faz 1–3 gerçek cihazda test edildi ve geçti.** Akış (gönderi görme/beğenme/yorum/paylaşma/"Talep Et"), bağlantı kurma (istek → kabul → telefon numarasının görünür olması → gizli gönderinin akışta belirmesi) ve mesajlaşma (canlı gelen mesaj + okundu bilgisi 5 sn içinde) telefonda doğrulandı. Türkçe karakterler cihazda doğru görünüyor — test sırasında görülen `?` işaretleri yalnızca asistanın git-bash üzerinden curl ile gönderdiği mesajlarda oluşan kodlama bozulmasıydı, uygulamayla ilgisi yok (veritabanında byte düzeyinde doğrulandı). **Not:** Admin ile olan test sohbetinde bu bozuk iki mesaj duruyor; istenirse silinebilir.
- 2026-09-13: **Faz 3 — İçerik akışı (feed)** tamamlandı. `Post` / `PostLike` / `PostComment` modelleri, `/api/posts` (akış, oluştur, sil, beğen/geri al, yorum ekle/listele/sil, fotoğraf). Görünürlük: `public` herkese, `connections` sadece yazarın bağlantılarına — ve bu kural sadece listede değil yorum/beğeni/fotoğraf uçlarında da uygulanıyor. Gönderiye sadece KENDİ firmasının ürünü iliştirilebiliyor (başkasının ürünüyle talep toplanmasın diye), ürün silinirse gönderi ayakta kalıp bağlantı boşalıyor. Fotoğraflar liste yanıtında dönmüyor (bir sayfa megabaytlara çıkıyordu); `hasImage` ile işaretlenip kart görünürken tek tek çekiliyor ve önbelleğe alınıyor. Mobilde: akış ekranı (sonsuz kaydırma, aşağı çekip yenileme, iyimser beğeni), gönderi oluşturma (fotoğraf + görünürlük + ürün seçici), yorumlar ekranı, firma baş harfi + doğrulama rozeti. 46 otomatik uçtan uca kontrol geçti (görünürlük sızıntısı, yan yollardan erişim, ürün iliştirme kuralı, imleçli sayfalama, `http://` izleme pikseli reddi).
- 2026-09-13: **Faz 2 — Mesajlaşma** tamamlandı. `Conversation` + `Message` modelleri (userAId < userBId kanonik sıralama), `/api/conversations` (başlat/listele/mesajlar/gönder/okundu/okunmamış-sayısı). Sohbet başlatmak ve yazmak için kabul edilmiş bağlantı şart, okumak için değil. Gerçek zamanlılık polling ile (sohbet 4sn, liste 15sn; uygulama arka plandayken duruyor). Mobilde: Mesajlar listesi (okunmamış rozeti, istemci tarafı arama), sohbet ekranı (iyimser gönderim + hata durumunda tekrar dene), yeni sohbet için bağlantı seçici, profilde "Mesaj Gönder", ana ekranda okunmamış sayaçlı "Mesajlar" chip'i. Uçtan uca curl ile doğrulandı (yabancı sohbete erişim 403, bağlantısız kişiye mesaj 403, boş mesaj 400, bozuk `since` 400, Türkçe karakterler byte-birebir).
- 2026-09-13: **Faz 1 — Bağlantı sistemi + Profil sayfası** tamamlandı. Yeni `Connection` modeli (pending/accepted, reddetme = satır silme), `POST/PATCH /api/connections`, `GET /api/connections(?status=)`, `GET /api/connections/status/:userId`, `GET /api/users/:id` (bağlantı yoksa telefon alanı gizli). Mobilde: Profil ekranı (bağlantı kur/kabul/reddet butonları), Bağlantılarım ve Bağlantı İstekleri ekranları, firma sayfasında tıklanabilir çalışan listesi, gelen numune taleplerinde talep edenin adı artık profile bağlıyor. Uçtan uca curl ile doğrulandı (istek/kabul/red/kendine-istek-engeli/tekrar-istek-engeli/telefon gizliliği). Plan: `C:\Users\ebosc\.claude\plans\swift-rolling-kazoo.md`.
