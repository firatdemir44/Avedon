// Sunucudaki MAX_PRODUCT_IMAGES ile aynı (backend/src/validation.ts).
export const MAX_PRODUCT_IMAGES = 6;

// Tek ürün fotoğrafının data URL uzunluğu (backend/src/validation.ts
// MAX_PRODUCT_IMAGE_CHARS ile aynı).
export const MAX_PRODUCT_IMAGE_CHARS = 700_000;

// Kumaş pasaportu sınırları (backend/src/passport.ts ile aynı).
export const MAX_COMPOSITION_ROWS = 6;
export const MAX_YARNS = 6;
export const MAX_CERTIFICATES = 10;
export const MAX_TEST_REPORTS = 10;
