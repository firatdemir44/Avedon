# Avedon mobil tasarım rehberi — C · Pazar Masası

Onaylanan tasarım yönü (2026-09-14): **C · Pazar Masası** — alım satım yapan için yoğun, bilgi önde bir B2B düzen. Taslaklar `docs/tasarim-yonleri/` (tuval: `canvas.json`), tokenlar `src/theme/index.ts`. Uygulama aşamaları: 1 yazı tipi · 2 renk ve üst bant · 3 yükleniyor/boş/hata durumları · 4 basma geri bildirimi · 5 ekran düzenleri. Denetim: `design-baseline.json`.

Yeni bir ekran yazarken önce buradaki kalıplardan birini kullan; kalıp yoksa en yakın taslağa bak. Tek seferlik stil yazmadan önce ortak bileşene ekle.

## Tokenlar (`src/theme/index.ts`)

| Grup | Değerler | Kural |
|---|---|---|
| Zemin / yüzey | `background` #EEF1F4 (gri zemin), `surface` #FFFFFF (bloklar), `surfaceTonal` (giriş alanları) | Ekran gri, içerik beyaz bloklarda. Gölge yok. |
| Marka | `primary` #133C5F (üst bant, birincil düğme, kodlar), `accent` #2696C6 (firma adları, bağlantılar, okunmamış) | |
| Asistan | `assistant` #A34F2E (kök boya kızılı), `assistantSoft` #F6E9E3 | **Yalnızca** asistanın olduğu yerde: seçili sekme ikonu, asistan avatarı, gönder düğmesi, asistan rozetleri (bkz. Asistan bölümü). |
| Çizgiler | `border` (kutu çerçevesi), `divider` (satır arası), `borderStrong` (çerçeveli düğme, bekleyen adım) | |
| Durum | `success`/`successSoft` (stokta, teslim), `warning`/`warningSoft`/`warningDot` (az stok, süreç sürüyor), `danger`/`dangerSoft`, `notification` (kırmızı sayı rozeti) | Renk tek başına anlam taşımaz; yanında metin olur. |
| Etkileşim | `pressed` #E4E9EE (basılı satır zemini), `chevron`, `chip`, `onPrimaryMuted` | |
| Boşluk | `gutter` 14 (satır/blok yatay iç boşluğu), `blockGap` 8 (bloklar arası gri aralık), `xs/sm/md/lg/xl` 4/8/16/24/32 | |
| Köşe | `md` 6 (düğme, alan, görsel, avatar), `sm` 4 (etiket/rozet), `lg` 8 (baloncuk), `pill` yalnızca sayı rozeti ve nokta | Hap biçimli düğme yok. |
| Yazı | IBM Plex Sans (metin) + **IBM Plex Mono** (ürün kodu, gramaj/en/stok, saat, tarih, telefon, VKN) | `fontWeight` kullanılmaz; kalınlık ayrı yazı tipi dosyasıyla (`fonts.semibold` vb.). **Ölçek (2026-09-15, kullanıcı "yazı küçük" dedi, taslaktan ~2px büyük):** title 24 · heading 19 · subtitle 17 · body 16 · label 15 · caption 13 · mono 15 · monoStrong 17. En küçük metin 11 (rozet/etiket). |
| Dokunma | `MIN_TOUCH` 44 | Her dokunulabilir öğe en az 44px (ya da `hitSlop`). |

## Düzen kalıpları

- **Blok:** kenardan kenara beyaz alan, köşesiz, gölgesiz. Bloklar arasında `blockGap` gri aralık. Kutu yığını (köşeli, aralıklı beyaz kartlar) kullanılmaz.
- **Bölüm başlığı:** `SectionHeader` — bloğun üstünde küçük gri başlık, isteğe bağlı eşit aralıklı sayı: "Ürünler (12)".
- **Çizgili satır:** `ListRow` (başlık, alt başlık, sol öğe, sağ ek bilgi, ok). Son satırda çizgi yok (`divider={index < n - 1}`).
- **Arama çubuğu:** beyaz şerit içinde `SearchField` (Ürünler, Mesajlar).
- **Filtre (Ürünler, Aşama A):** arama çubuğunun yanında 44px kare filtre düğmesi (etkin filtre varsa lacivert dolu + kırmızı sayı rozeti); görünüm seçiminin altında yatay kaydırılan etkin filtre çipleri (açık mavi, "×" ile tek tek kaldırılır, sonda kırmızı "Temizle"). Filtre ekranı başlıklı beyaz bloklar + altta sabit "Temizle / Filtreyi Uygula".
- **Klasör içi alt çeşit çipleri:** klasör geri satırının altında yatay çipler "Süprem 3"; sayı eşit aralıklı yazıyla.
- **Logo:** `assets/brand/avedon-logo-light.png` (harfler beyaz) lacivert zeminde, `avedon-logo.png` açık zeminde, `avedon-mark.png` yalnızca girdap işareti. Kaynak: `docs/orijinal-tasarim/logo` (repoya girmez). Logo metinle yeniden çizilmez, dosya kullanılır.
- **Kumaş pasaportu formu (Faz 1):** kompozisyon satırları (lif çipi + oran alanı, satır ekle/sil, toplam 100 değilse turuncu not; kaydı engellemez); Ticari bölümü (MOQ + birimi stok biriminden bağımsız, termin gün, fiyat + para birimi + birim; "yalnızca size görünür" notu); İplik ve Sertifikalar bölümleri `CollapsibleSection` ile kapalı gelir. Sunucu uyarısı (makullük) varsa kayıt biter ama ekranda kalınır: sarı kutu "Kaydedildi. Dikkat:" + notlar, kaydet düğmesi "Devam" olur (web'de Alert yok). En tipi "Tüp en" seçilince altında ikinci seçim çıkar ("Girdiğiniz en neyi gösteriyor?": Tek yüz tüp eni / Açık en, varsayılan seçili DEĞİL) ve altında canlı "Hesap eni: 160 cm (80 × 2)" satırı; seçilmezse `widthMeaning` boş gider ve hesap eni girilen en olur, ürün sayfasında tek yüz tüp eninde "80 cm tek yüz tüp eni · hesap eni 160 cm" satırı, `PassportCard`'da En sütununun altında "tüp, tek yüz" ipucu çıkar. Ürün sayfasında sahibine sarı onay şeridi: "... alanı metinden otomatik çıkarıldı. Doğru mu?" + Onayla / Düzenle.
- **Etiketten doldur (Faz 1, Adım 3):** ürün formunda fotoğraf bölümünün altında outline düğme (`scan-outline`), basınca ekran içinde `ListRow` kaynak listesi (Fotoğraf çek · Galeriden seç · PDF seç · Metin yapıştır; web'de pencere yok). Sürerken düğme "Etiket okunuyor..." + ActivityIndicator, form kilitlenmez. Sonuç `PassportReviewScreen` ("Etiketten Okunanlar"): her okunan alan bir satır, sol onay kutusu, alan adı, okunabilir değer, güven rozeti (≥0,9 yeşil "Etikette yazıyor" · 0,6-0,89 turuncu "Kontrol edin" · <0,6 kırmızı "Şüpheli", varsayılan işaretsiz), altında gri "Okunan: ..." kanıtı; "Okundu ama aktarılmadı" bölümü (ham değer + neden), "Notlar", sarı "Dikkat" kutusu. Altta sabit çubuk: "Vazgeç" + "Forma aktar (N)". Aktarım `navigation.popTo('AddProduct', ..., { merge: true })` ile forma GERİ döner; aktarılan alanlar kayıtta `fieldMeta` (source `extracted`, confirmed) olarak gider, kullanıcı elle değiştirirse satır düşer.
- **Akış pasaport kartı (Faz 1, Adım 6):** taslak `docs/tasarim-2027/Main.dc.html`. Ürünlü gönderide görselin altında `PassportCard`: beyaz kutu (ince `border` çerçeve, 4px iç boşluk) içinde **kesik çizgili** (`borderStrong`, dashed) iç çerçeve. Üst satır: mono ürün kodu (lacivert) solda, sağda 11px gri harf aralıklı "KUMAŞ PASAPORTU". Ortada üç eşit sütun (11px gri etiket + mono değer): Gramaj `220 gr/m²` · En `150 cm` (+ en tipi varsa altında küçük gri "açık en"/"tüp en") · Örgü (dokuma ve diğerinde "Çeşit"; değer alt çeşit etiketi, yoksa çeşit etiketi). Alt satır `divider` çizgisinin altında: kompozisyon metni (`formatComposition`, mono) solda, sağda stok noktası + `1.200 m · MOQ 300 m` (+ termin varsa `· 12 gün`). Sertifika varsa en altta yeşil tonlu küçük rozetler (`certificateLabel`). Karta dokunma ürün sayfasını açar. **Eski ölçü şeridi kalktı**, yerini bu kart aldı.
- **Ürün sayfası pasaport kartı (2026-09-17):** galerinin altında akıştakiyle **aynı** `PassportCard` (`onPress` verilmez, dokunulamaz). Eski kod + kategori + stok rozeti satırı ve kartla tekrar eden özet satırları (Çeşit, Alt çeşit, Stok, Ağırlık, Genişlik, En tipi) kalktı; özet bloğunda yalnızca kartta olmayanlar kaldı (Kullanım, kompozisyon satırı yoksa İçerik metni, Not) ve hepsi boşsa blok hiç çizilmiyor. Altındaki ayrıntı bölümleri (Kompozisyon, Ticari, Apre, Sertifikalar, İplik, Test raporları, onay şeridi) aynen duruyor. Eşleme tek yerde: `toPassportCardProduct` (`components/PassportCard.tsx`), akış kartı da onu kullanıyor; kaynak arayüz `PassportCardSource` hem `FeedProduct`'ı hem `ProductDetail`'i kapsıyor (sertifika adları yoksa `certificates[].name`'den alınır).
- **Ürün formunda test raporları (2026-09-17):** Sertifikalar'ın altında `CollapsibleSection` "Test raporları" (kapalı gelir, veri varsa açık). Satır: **Test türü** serbest metin (80 karakter) + altında öneri çipleri (Çekme · Boncuklanma (pilling) · Renk haslığı · Yıkama sonrası boyut değişimi · Gramaj · Patlama mukavemeti; `ChipSelect` değil, çünkü değer listeyle sınırlı değil), **Sonuç** (200 karakter), **Test tarihi** (YYYY-AA-GG, isteğe bağlı) ve **Belge fotoğrafı** (sertifikadaki desen: önizleme + Fotoğraf Ekle/Değiştir/Kaldır, değişmeyen satır `{ existing: position }` ile gider). En fazla 10 satır (`limits.ts` `MAX_TEST_REPORTS`). Kayıtta **tam liste** gönderiliyor: hepsi silinirse boş dizi gider ve sunucudakiler silinir (önceden form test raporlarına hiç dokunmuyordu). Türü boş ama başka alanı dolu satır kaydı engeller ("Her test raporu satırında test türünü yazın."); tamamen boş satır sessizce atılır. Etiketten doldur aktarımı test raporlarına dokunmaz.
- **Akış eylem çubuğu:** ürünlü gönderide ticari eylemler: `PrimaryButton size="sm"` "Numune talep et" (esnek genişlik) · outline "Takibe al" / dolu "Takipte" (`bookmark-outline`/`bookmark`) · 44px kare outline "Teklif iste" (`pricetag-outline`) · yorum sayacı. **Ürünlü gönderide beğeni gösterilmez** ("Takibe Al" onun yerine geçti; kullanıcı kararı 2026-09-16), yorum kalır. Kendi firmasının ürününde "Teklif iste" hiç çıkmaz. "Teklif iste" **teklif isteği formunu açar** (Faz 2, Adım 2); eski sohbet açan akış, kart içindeki sonuç kutusu ve bağlantı sorusu kalktı (bağlantı artık şart değil). Ürünsüz duyuruda çubuk eskisi gibi: beğeni · yorum · paylaş.
- **Konfeksiyon maliyeti (kalem kalem, 2026-09-17):** `GarmentCostCalculator` eski 4 alanlı halinden çıktı; Fırat'ın kararı (`docs/faz1-plani.md` Adım 4, madde 3) gereği kalemler ayrı. Bölümler: Para birimi (`ChipSelect` TRY/USD/EUR, yalnızca etiket, kur çevirmez) · Kumaş (tüketim, metre fiyatı, kesim firesi) · Adet başı kalemler (Kesim, Dikim, Yıkama / baskı / boya, Aksesuar + altında küçük gri açıklama, Paketleme, Nakliye, Genel gider / fire / diğer) · Sipariş adedi (isteğe bağlı). Sonuç `ResultCard`: yalnızca dolu kalemler satır olur, her satırın altında "Toplamın %N'i" notu, en büyük kalemde ek olarak "en büyük kalem" (`highlight`), sonra çizgiyle ayrılmış "Adet maliyeti" (`strong`) ve varsa "Sipariş toplamı"; altta gri "Boş kalemler: paketleme, nakliye". Kalem adları ve sırası sunucudaki beceriyle (`backend/src/skills/calc/garmentCost.ts`) aynı; ortak yardımcı `src/features/calculators/garmentItems.ts` (kumaş maliyeti hâlâ `formulas.ts`'teki `calculateGarmentCost`, o dosyaya dokunulmaz). `ResultCard` satırına `note` / `strong` / `highlight` eklendi, diğer hesaplayıcılar etkilenmez.
- **Görünüm seçimi (Ürünler, Akış):** arama şeridinin altında (Akış'ta üst bandın hemen altında) iki eşit düğme, seçili olan lacivert dolu. Akış'ta "Tümü" / "Bağlantılarım"; ikincisi boşsa `EmptyState` "Henüz bağlantınız yok ya da bağlantılarınız paylaşım yapmadı." + "Bağlantı bul". "Tümü" tek akış, "Çeşitler" kumaş çeşidi klasörleri (klasör satırı: açık mavi kare içinde klasör ikonu + "N ürün"); açık klasörün üstünde geri satırı. Seçim cihazda hatırlanır. Arama yazılınca klasörler yerine sonuçlar gelir.
- **Başlık eylemleri:** `HeaderButton` — lacivert bantta yalnızca ikon (Akış: danışman, paylaş) ya da ikon + kısa etiket (Mesajlar: + Yeni). Düz metin eylem yok.
- **Sabit eylem çubuğu:** ekranın altında beyaz, üst çizgili, `PrimaryButton size="lg"` (Ürün sayfası: Firma + Numune Talep Et; Numune takibi: adımı işaretle). Alt güvenli alan `useSafeAreaInsets` ile çubuğun kendisine eklenir.
- **Adım adım kurulum (2026-09-17, "Firma sayfanı tamamla"):** uzun tek formun yanına konan kılavuzlu akış (`CompanySetupScreen`, rota `CompanySetup: { step? }`). Üstte beyaz blokta **ilerleme**: adım sayısı kadar eşit parçalı ince çubuk (tamamlanan parça lacivert dolu, bulunulan ama tamamlanmamış parça `accentSoft` + lacivert çerçeve, kalanlar `divider`), altında mono "2 / 5" + adım adı. Ortada **yalnızca o adımın alanları**; altta **sabit eylem çubuğu**: solda outline "Atla", sağda dolu "Kaydet ve devam" (son adımda "Bitir"), ikisi de `size="lg"` ve eşit genişlikte, alt güvenli alan çubuğun kendi iç boşluğuna eklenir. Her adım **yalnızca kendi alanlarını** `PATCH` eder (değişiklik yoksa istek atılmaz), başarıda `haptics.success()`, hata satır içi `InlineError`. Ekran açılınca veri yüklenir (iskelet); `step` verilmediyse **tamamlanmamış ilk adımdan** başlar. Bitince `popTo` ile firma sayfasına döner. Adım durumu ve yüzde tek yerde: `features/companies/completeness.ts` (tanıtım ≥40 karakter + şirket tipi · e-posta ya da telefon + şehir · logo · en az 1 ofis fotoğrafı · en az 1 ürün). Giriş noktası firma sayfasının üstündeki açık mavi şerit ("Firma sayfanız %40 tamamlandı" + ince çubuk + "Tamamla"); %100'de şerit görünmez. Hakkında sekmesindeki boş durum metni ilgili adıma götürür (`{ step: 'tanitim' }`). "Firmayı Düzenle" tek form olarak duruyor (ileri kullanıcılar için); ortak parçalar: `CompanyLogoPicker`, `features/companies/useCompanyGalleries.ts`, `features/companies/validation.ts`.
- **Yazma alanı:** tonlu giriş kutusu + 44px kare lacivert gönder düğmesi (Sohbet, Yorumlar).

## Ortak bileşenler

| Bileşen | Ne için |
|---|---|
| `ListRow` | Menü, kişi, hesaplama, çalışan satırları |
| `ProductRow` | Ürün listesi satırı (Ürünler, Firma sayfası): 64px görsel, kod + tip etiketi, firma, içerik, ölçüler + stok, "Talep Et" |
| `StockIndicator` | `StockDot` / `StockValue` / `StockBadge`; **100 m altı "az stok"** (turuncu) |
| `SampleStatusBadge` | Numune durumu: talep edildi mavi · onaylandı/hazırlandı turuncu · teslim yeşil; metin sunucudan |
| `QuoteStatusBadge` | Teklif isteğinin durumu: bekleniyor mavi · teklif verildi turuncu · kabul yeşil · ret kırmızı · geri çekildi gri; metin bileşenin içinde (`quoteStatusLabel`) |
| `PrimaryButton` | `primary` / `secondary` / `outline`, `sm` (44, dar boşluk + `label` ölçekli tek satır yazı) / `md` (44) / `lg` (48), isteğe bağlı `icon`; `tone="assistant"` çerçeveli düğmeyi asistan kızılına çevirir (yalnızca asistana giden düğmelerde) |
| `assistant/ChatParts` | Asistan sohbetlerinin ortak parçaları: `ChatDayChip`, `UserBubble`, `AssistantBubble`, `ThinkingBubble`, `AssistantComposer`, `chatStyles`. Firma asistanı ve satıcı asistanı ekranları aynısını kullanır |
| `PassportCard` | Kumaş pasaportu kartı: kod + "KUMAŞ PASAPORTU", gramaj/en/örgü sütunları, kompozisyon + stok/MOQ/termin, sertifika rozetleri. Akış kartı kullanıyor; ürün sayfası ve asistan sonuçları da aynı kartı kullanabilsin diye ayrı bileşen (sunucu tipine değil sade `PassportCardProduct` arayüzüne bağlı) |
| `CompanyLogoPicker` | Logo seçme/kaldırma bloğu (96px önizleme ya da baş harf + "Logo Seç" / "Logoyu Kaldır"); 400px genişlikte sıkıştırma. "Firmayı Düzenle" ve adım adım kurulumun logo adımı aynı bileşeni kullanır |
| `CompanyAvatar` | Kare (6px) lacivert baş harf ya da firma logosu |
| `Skeleton` | `SkeletonList` / `SkeletonDetail`; yeni düzendeki listeler **blok** (tek beyaz blokta çizgili) ya da **yığın** (akış) iskeleti kullanır |
| `StateView` | `EmptyState`, `ErrorState`, `InlineError` |
| `ProductGallery` | Ürün sayfası kaydırmalı fotoğraf galerisi: yalnızca görünen ve komşu fotoğraflar çekilir; sayaç "2/4", noktalar, `overlay` ile üstte düğme (takip işareti, `bookmark`) |
| `MultiChipSelect` | Çoklu seçim çipleri (kullanım amaçları); seçili çip lacivert + onay işareti. Tek seçim için `ChipSelect` |
| `PassportReviewScreen` satırı | Onay kutulu alan satırı + güven rozeti + "Okunan:" kanıtı (yalnızca bu ekranda; ikinci kullanımda ortak bileşene çıkarılır) |
| `CollapsibleSection` | SectionHeader görünümünde dokunulabilir başlık + chevron; varsayılan kapalı gelen form bölümleri (ürün formunda İplik, Sertifikalar; içinde veri varsa açık gelir) |
| `AssistantAvatar` | Asistanın çizim yüzü (İpek / Mert): düz geometrik büst, `size` ve `state` (`idle`/`listening`/`thinking`/`speaking`/`result`). `react-native-svg`; animasyon `Animated` + zamanlayıcı, "hareketi azalt" açıksa durur |
| `PersonaPicker` | Asistan karakteri seçimi: iki kart yan yana (avatar, ad, tanıtım); ilk açılışta ve "Değiştir"de aynı kartlar |
| `ResultCard` | Hesaplayıcı ekranlarının tonlu sonuç kutusu (etiket + değer). Aynı dosyadaki **`AssistantResultCard`** asistan sohbetindeki araç sonucu kartıdır (beyaz kutu, kesik çizgili iç çerçeve, büyük harfli başlık + birim, satırlar, katlanabilir "Nasıl hesaplandı") |

## Asistan (Faz 1, Adım 5)

Taslak: `docs/tasarim-2027/Asistan.dc.html`. Alt menüdeki **"Hesaplamalar" sekmesinin yeri "Asistan" oldu** (ikon `sparkles-outline` / `sparkles`, rota adı `AssistantTab`); hesaplayıcı listesi yığına taşındı (`CalculatorsList`) ve asistandaki "Tüm hesaplayıcılar" çipinden açılıyor. Eski `Advisor` ekranı ve `askAdvisor` kaldırıldı; Akış'taki ✦ düğmesi asistan sekmesine gidiyor.

- **Renk kuralı:** `colors.assistant` (#A34F2E kök boya kızılı) ve `colors.assistantSoft` **yalnızca asistanın kendisinin olduğu yerde** kullanılır: seçili Asistan sekme ikonu, sohbetteki 28px asistan avatarı, gönder düğmesi, hafıza öneri kartının "Kaydet" düğmesi, "hesaplıyor" göstergesi. Başka hiçbir ekranda, düğmede ya da durumda geçmez; ikincil renk her yerde lacivert/mavi kalır.
- **Üst bant:** başlık "Firma asistanı", altında küçük `onPrimaryMuted` satır "Firma adı · İpek" (seçili karakterin adı). Sağda iki `HeaderButton`: firma hafızası (`settings-outline`) ve sohbetler (`time-outline`). Beceri sayısı (`GET /api/skills`) karşılamanın altındaki ipucu satırına taşındı.
- **Sohbet:** kullanıcı balonu lacivert dolu sağda, saat mono; asistan solda 38px `AssistantAvatar` + beyaz ince çerçeveli balon. Gün ayracı sohbetteki gibi (`formatDayLabel`).
- **Sonuç kartı (`AssistantResultCard`):** balonun altında, her araç çağrısı için bir kart. Beyaz kutu içinde kesik çizgili (`borderStrong`) iç çerçeve, üstte küçük gri BÜYÜK HARFLİ başlık ve sağda birim, altında `divider` çizgili satırlar; sayılar `IBM Plex Mono`, `toLocaleString('tr-TR')`. Son/öne çıkan satır kalın ve lacivert. Altta katlanabilir "Nasıl hesaplandı" (becerinin formülü). **Karttaki her rakam araç çıktısından gelir, model metninden değil** (`features/assistant/toolResult.ts`); tanınmayan beceri sunucunun `summary` metnini gösterir.
- **İzleme kartı (Faz 2, Adım 1):** hafıza kartının aynısı — "İzleme kurulsun mu?" + kural adı + gerekçe; "İzlemeye al" (`createWatchRule`, başarıda kart yeşil "İzlemeye alındı: ...") ve "Şimdi değil". Sunucudan `message.watchSuggestions` ile gelir; asistan izlemeyi kendisi KURMAZ. Durum anahtarı `${mesajId}:watch:${sıra}`. Sınır dolmuşsa (`too_many_rules`) kart içinde açıklama.
- **Hafıza kartı:** "Hafızaya kaydedilsin mi?" + `label: değer` + gerekçe; "Kaydet" (PUT memory, başarıda kart yeşil "Kaydedildi" olur + `haptics.success()`) ve "Şimdi değil" (kart kapanır). Durum mesaj kimliği + anahtar ile ekranda tutulur. Asistan hafızaya kendisi yazmaz.
- **Çipler:** girdi çubuğunun üstünde yatay kaydırılan beceri çipleri (İplik çevir · Üretim hesabı · Kumaş maliyeti · Konfeksiyon maliyeti · Ürün izle) girdi kutusuna başlangıç metni yazıp odaklanır; "Tüm hesaplayıcılar" `CalculatorsList` ekranını açar. Sohbet boşken karşılama metni + örnek soru çipleri.
- **Girdi çubuğu:** tonlu çok satırlı kutu ("Sor, hesaplat, etiket yapıştır...") + 44px kare kızıl gönder düğmesi (`arrow-up`). Gönderirken kullanıcı balonu hemen eklenir, altında "Hesaplıyor..." göstergesi; hata olursa `InlineError` + "Tekrar dene" (yanıt 5-30 sn sürebilir, istek zaman aşımı 120 sn).
- **Sohbet kaydı sunucuda:** cihazda yalnızca son `threadId` durur (`features/assistant/threadStore.ts`). `AssistantThreads` ekranı sohbetleri listeler, "Yeni sohbet" satırı ve çöp ikonuyla silme (`confirmAction`) sunar; seçilen sohbeti cihaza yazıp geri döner, asistan ekranı odaklanınca okur (iç içe sekme parametresi taşınmaz). Silme düğmesi satırın **yanında**, içinde değil (web'de iç içe `<button>` olmasın).
- **Firma hafızası ekranı (`AssistantMemory`):** bilinen anahtarlar `ListRow` olarak (etiket + ipucu + değer ya da "Kayıtlı değil"); dokununca satır düzenleme bloğuna dönüşür (sayı/metin alanı, Kaydet / Vazgeç / Sil). Firması olmayan kullanıcıda (403) açıklama metni. En üstte "Asistan" bölümü (aşağıda).

### Asistan kişiliği ve avatar (Faz 1, Adım 9)

Karar: `docs/yol-haritasi.md` §5. İki karakter, **İpek** ve **Mert**; kullanıcı ilk açılışta seçer, sonra değiştirir. Uygulama cinsiyet sormaz. Yüz çizimdir; **video ve ses yok**.

- **Avatar (`AssistantAvatar`):** düz (flat) geometrik büst, `react-native-svg`. Yuvarlak yüz, nokta gözler, sade ağız; İpek omuz hizası düz saç (kırmızımsı kahve), Mert kısa saç (koyu kahve). Ten ve saç tonları bileşen içinde sabit; **tema renklerinden yalnızca yaka/aksesuar `colors.assistant`**, gövde lacivert, çerçeve `assistantSoft` (renk kuralı: asistan kızılı yalnızca asistanın kendisinde).
- **Haller:** `idle` 3-4 sn'de bir göz kırpma · `listening` hafif yukarı aşağı salınım · `thinking` gözler yana bakar + üstte üç nokta baloncuğu · `speaking` ağız açılıp kapanır · `result` küçük gülümseme + baş hafif eğik. SVG özellikleri animasyonlanmaz: kare durum değiştikçe yeniden çizilir, yalnızca salınım `Animated` ile (web'de `useNativeDriver: false`). "Hareketi azalt" açıksa hareket yok.
- **Durum bağlantısı:** girdi kutusuna yazarken `listening`, gönderildi ve beklenirken `thinking` ("Hesaplıyor..." göstergesiyle birlikte), yanıt geldiğinde araç kartı varsa `result` yoksa `speaking`, 2,6 sn sonra `idle`. Canlı olan **yalnızca son asistan mesajının** avatarıdır.
- **Seçim (`PersonaPicker`):** `GET /api/assistant/persona` `persona: null` dönerse sohbet yerine seçim görünümü: başlık "Kim yardımcı olsun?", iki kart yan yana (büyük avatar, ad, tanıtım cümlesi); dokununca `PUT /api/assistant/persona`, `haptics.success()` ve karşılamaya geçilir. Seçili kart kızıl çerçeveli + onay işareti. Sunucuya ulaşılamazsa seçim ekranında kilitlenilmez, varsayılan karakterle sohbet açılır.
- **Karşılama:** sohbet boşken üstte 112px avatar + asistan balonunda `GET /api/assistant/greeting?hour=` metni (modelsiz, anında; cihaz saati gönderilir). **Sohbet kaydına yazılmaz**, yalnızca gösterilir; altında "…N beceri hazır." ipucu ve mevcut örnek soru satırları kalır.
- **Değiştirme:** "Firma hafızası" ekranının en üstünde "Asistan" bölümü: seçili avatar (48px) + ad + tanıtım, sağda outline "Değiştir" (aynı iki kart açılır). Kişilik kullanıcıya bağlı olduğu için firması olmayan kullanıcıda ve hafıza yüklenemediğinde de görünür.

## Satıcı asistanı (Faz 2, Adım 3)

Plan: `docs/faz2-plani.md` Adım 3. Alıcı, **başka bir firmanın** asistanıyla konuşur; asistan yalnızca o firmanın yayınlanmış kataloğundan ve sık sorulanlarından cevap verir, **fiyat vermez**. Sunucu `backend/src/routes/assistant.ts` "Satıcı asistanı" bölümü + `backend/src/assistant/buyer.ts`; istemci uçları `api/client.ts` "Satıcı asistanı" bölümünde.

- **Ortak sohbet parçaları `src/components/assistant/ChatParts.tsx`:** `ChatDayChip`, `UserBubble`, `AssistantBubble`, `ThinkingBubble`, `AssistantComposer` (tonlu kutu + 44px kare kızıl gönder düğmesi, isteğe bağlı çip şeridi) ve `chatStyles` (liste, asistan satırı, örnek soru kutusu). **Hem `AssistantScreen` hem `SellerAssistantScreen` bunları kullanır**; balon/çubuk stili kopyalanmaz.
- **`SellerAssistant: { companyId, companyName?, productCode? }`:** başlık "<Firma> asistanı" (ad yoksa "Firma asistanı"; iplik açılınca sunucudan gelen adla düzelir). Üstte açık gri **bilgi şeridi**: "Bu asistan yalnızca firmanın yayınlanmış kataloğundan cevap verir. Fiyat için Teklif iste'yi kullanın." Avatar yerine `CompanyAvatar` + sağ altında **asistan rengi** küçük kıvılcım rozeti; **renk kuralı burada da geçerli** (kızıl yalnızca gönder düğmesi, rozet ve "Asistana sor" bağlantısı). Kişilik seçimi, hafıza/izleme kartları ve beceri çipleri BU EKRANDA YOK.
- Boş sohbette kısa karşılama balonu + örnek soru satırları ("Elastanlı tülünüz var mı?", "MOQ ve termin nedir?", "OEKO-TEX sertifikalı ürünleriniz hangileri?"); `productCode` verildiyse ilk satır "<kod> hakkında bilgi alabilir miyim?".
- **`katalog_ara` kartında ürün satırı dokunulabilir** (ürün sayfası açılır): `ResultRow.productId` + `AssistantResultCard`'ın `onProductPress`'i; satır 44px ve sonda ok. Kartın altında "Teklif iste"/"Numune talep et" düğmesi **yok** (ürün sayfasından yapılır). `soruyu_ilet` kartının altında mavi bilgi kutusu: "Sorunuz firmaya iletildi. Cevap gelince bildirim alacaksınız." 429 `daily_limit` → "Bugünlük soru sınırına ulaştınız." (tekrar denenmez).
- **Giriş noktaları:** başka firmanın sayfasında kimlik bloğunda outline "Asistana sor" (`sparkles`, `PrimaryButton tone="assistant"` — yazı, ikon ve çerçeve kızıl); ürün sayfasında firma satırının altında küçük "Asistana sor" bağlantısı (`productCode` ile). Kendi firmasında/ürününde hiç çıkmaz.
- **Gelen sorular (`CompanyQuestions`):** üstte açıklama, "Bekleyen sorular (N)" bölümü; satır soru + soran (ad · firma) + ürün kodu + göreli zaman, dokununca **satır içinde** çok satırlı cevap alanı + "Sık sorulanlara ekle" onay kutusu + "Cevabı gönder". Cevaplananlar altta gri blokta cevabıyla. Başlıkta `HeaderButton` "Sık sorulanlar". Giriş: Firmam sayfasında outline "Asistana gelen sorular (N)" ("Gelen teklif istekleri" deseni, sayı odakta tazelenir) ve bildirimden. Firması olmayanda 403 → açıklama.
- **Sık sorulanlar (`CompanyFaq`):** üstte "Asistanınız alıcı sorularını bu cevaplara göre yanıtlar. Fiyat yazmayın; fiyat yalnızca teklifle gider." Sonra "Yeni soru ekle" satırı, altında liste: satıra dokunmak düzenleme formunu (Soru / Cevap + Kaydet / Vazgeç / Sil) açar; çöp ikonu satırın **yanında** (web'de iç içe düğme olmasın), silme `confirmAction`. 409 `too_many_faqs` satır içi hata. Giriş: `AssistantMemory` ekranındaki "Alıcı soruları > Sık sorulanlar" satırı ve `CompanyQuestions` başlığındaki düğme.
- **Bildirimler:** `company_question_new` (satıcıya) → `CompanyQuestions`; `company_question_answered` (alıcıya) → `SellerAssistant` (`data.companyId`); ikon `sparkles-outline`.

## Makine parkı ve kapasite (Faz 2, Adım 5)

Plan: `docs/faz2-plani.md` Adım 5 + Fırat'ın 4. kararı (pus, fayn, sistem, en dahil bütün makine bilgileri; kapasite hem **parkur** hem beyan edilen **aylık tonaj**). Sunucu `backend/src/routes/machines.ts`; istemci uçları `api/client.ts` "Makine parkı ve fason kapasite" bölümünde. **Makine türü serbest metindir**: `GET /machines/kinds` yalnızca öneri döner (başlangıç listesi + platformda girilenler), kullanıcı istediğini yazar. Ortak etiket ve özet üretimi tek yerde: `features/machines/catalog.ts` (`machineSummary`, `machineOneLine`, `groupMachines`, grup etiketleri, özellik önerileri).

- **Firma sayfasının 5. sekmesi "Makine parkı":** sekme şeridi artık **yatay kaydırılabilir** (`ScrollView horizontal`, sekmeler `flexGrow: 1` ile dar ekranda taşmaz, geniş ekranda eşit bölünür); sekme sayısı 5 oldu. İçerik yalnızca sekme açıkken çekilir ve odakta tazelenir (elde veri varsa ekranda kalır). Üstte **kapasite bloğu**: "Aylık kapasite" etiketi + mono değer ("120 ton", yoksa "Bildirilmedi"), sağda fason rozeti (yeşil `successSoft` "Fason kapasitesi açık" / gri tonlu "Fason almıyor"), altında not ve 11px gri "güncellendi: 3 gün önce"; kendi firmanda outline `sm` "Düzenle". Altında gruplara göre `SectionHeader` ("Örme (8)", sayı = adet toplamı) + makine satırları: başlık `kind` + mono "× 6", alt satır `machineSummary` ("30 pus · 28 fayn · 96 sistem · Mayer Relanit · 2019 · tek plaka"; dokuma/boya/baskıda "240 cm en"), varsa not. Boşken: başkasının firmasında "Bu firma makine parkını henüz girmedi.", kendi firmanda boş durum + "Makine ekle".
- **Yönetim (`MachinePark`):** üstte kapasite formu (Aylık kapasite (ton), "Fason kapasitesi açık" `Switch`, Not, "Kaydet"; kaydedilince yeşil tek satır not). Form **yalnızca ilk veri gelince** doldurulur, odak yenilemesi yazılanı silmez. Altında "Makine ekle" satırı (`add-circle-outline`) ve gruplu makine listesi: satıra dokunmak `MachineForm`'u açar, çöp ikonu satırın **yanındadır** (web'de iç içe düğme olmasın) ve `confirmAction` sorar. Firması olmayanda açıklama.
- **Makine formu (`MachineForm: { machineId? }`):** Grup (`ChipSelect compact`), **Makine türü** serbest metin + altında o grubun öneri çipleri (dokununca alana yazar, yazdıkça süzülür; `ChipSelect` değil, çünkü değer listeyle sınırlı değil — ürün formundaki test türü deseni). Sonra Marka/Model, Yıl/Adet. Teknik alanlar gruba göre: **örmede** Pus (inç), Fayn, Sistem sayısı, İğne sayısı; **dokuma/boya-terbiye/baskıda** Çalışma eni (cm); diğerlerinde teknik bölüm hiç çıkmaz. En altta Özellik (öneri çipleriyle) ve Not. Sayı alanları virgüllü girişi kabul eder; gruba uymayan teknik alanlar `null` gider. Altta sabit çubukta "Kaydet"; başarıda `popTo('MachinePark')`. `too_many_machines` / `no_company` / `invalid_body` satır içi `InlineError`.
- **Kapasite araması (`CapacitySearch`):** süzgeç formu (Grup çipleri, Makine türü + öneriler, Fayn, Pus, En az çalışma eni, Şehir, "Yalnızca fason açık" `Switch`) + `lg` "Ara". Sonuç satırı: `CompanyAvatar` (doğrulama rozetiyle), firma adı (mavi), şehir, mono "Aylık 120 ton · fason açık" ve eşleşen makinelerin tek satır özetleri ("6× Yuvarlak örme (süprem) 30 pus 28 fayn"), sonda ok; dokununca firma sayfası **Makine parkı sekmesi açık** gelir (`CompanyProfile` rota paramı `initialTab`). Sonuç yoksa "Eşleşen firma yok" + süzgeci gevşetme önerisi. Kendi firmanız sonuçta çıkmaz (sunucu dışlar).
- **Girişler:** Profil menüsünde "Fason Kapasite Ara" ve (firması olanda) "Makine Parkım"; firma sayfasındaki Makine parkı sekmesinin "Düzenle" düğmesi; asistan beceri çipi "Fason kapasite" ("28 fayn 30 pus süprem örecek fason arıyorum" yazar).
- **Asistan kartı:** `kapasite_ara` satırları firma adı + mono ton değeri, altında "Bursa · fason açık · 6 makine · aylık 120 ton". Satır dokunulabilir: `ResultRow.companyId` + `AssistantResultCard`'ın `onCompanyPress`'i (ürün satırındaki `onProductPress` deseninin aynısı; kartta ikisi tek "bağlantı" dalıyla çiziliyor).

## Bildirimler ve izleme (Faz 2, Adım 1)

Plan: `docs/faz2-plani.md` Adım 1. **Yalnızca uygulama içi**; push bildirimi (Expo) bu adımda YOK, sayaç yoklamayla geliyor.

- **Zil:** Akış başlığının sağında `HeaderButton` (`notifications-outline`) + "Gönderi paylaş"; okunmamış varsa ikonun sağ üstünde `colors.notification` sayı rozeti (99'dan fazlası "99+"). Rozet `HeaderButton`'ın `badge` özelliğiyle geliyor, ayrı bileşen yok. Sayaç ekran **odaklanınca** ve uygulama ön plandayken **60 sn'de bir** tazelenir (`fetchUnreadNotificationCount`; `AppState.currentState !== 'active'` ise atlanır, hata sessiz).
- **Bildirimler ekranı (`Notifications`, yığın):** çizgili `ListRow` satırları. Sol öğe: okunmamışlarda `colors.accent` 8px nokta (okunmuşta görünmez ama yer kaplar ki başlıklar hizada kalsın) + türe göre ikon (`watch_match` `bookmark` · `sample_*` `cube-outline` · `connection_*` `people-outline` · tanınmayan tür `notifications-outline`). Sağda mono göreli zaman. Okunmamış satırın zemini `accentSoft`. Dokunma **hem** okundu yapar **hem** hedefe gider: ürün sayfası · numune takibi (id yoksa Gelen Talepler / Taleplerim) · Bağlantı İstekleri · profil. Okundu işaretleme iyimser (satır hemen değişir, hata şerit olarak düşer). Üstteki blokta "Tümünü okundu say" (yalnızca okunmamış varken) ve "İzlediklerim". Boş durum: "Henüz bildirim yok..." + "İzleme kur". Aşağı çekerek yenileme `useFocusLoad`'un `refresh`'i.
- **İzlediklerim (`WatchRules`, yığın):** girişler Profil menüsündeki "İzlediklerim" satırı ve Bildirimler ekranı. Üstte "Yeni izleme" satırı (`add-circle-outline`) → `ProductFilters` **izleme kipinde**; sınır doluysa o satırın yerine turuncu ikonlu açıklama gelir. Kural satırı: ad (başlık) + "N eşleşme · son: 3 gün önce · duraklatıldı" (alt başlık), yanında `Switch` (aktif/pasif, iyimser, hata geri alır) ve çöp ikonu (`confirmAction`). Anahtar ve çöp satırın **yanında**, `ListRow` dokunulamaz (web'de iç içe düğme olmasın).
- **İzleme kipi (`ProductFilters` + `mode: 'watch'`):** başlık "Yeni İzleme", alt düğme "Bu süzgeci izle" (kaydederken "Kuruluyor..."), "Temizle" kalır. Sunucudaki `watchQuerySchema` ürün süzgecinin **alt kümesi ve strict** olduğu için desteklenmeyen bölümler (Stok, İçerik, En tipi) bu kipte **hiç gösterilmez** — sessizce düşmesin. Başarıda `popTo('WatchRules')`; `too_many_rules` / `invalid_body` satır içi `InlineError`.
- **Ürünler ekranında "Bu aramayı izle":** etkin süzgeç çipleri varken şeridin sonunda `bookmark-outline` + metin bağlantısı. Arama metni `search` olarak kurala girer. Sonuç şeridin altında tek satır: yeşil "İzlemeye alındı." (izlemeye giremeyen süzgeç varsa parantezde söylenir) ya da kırmızı açıklama; arama/süzgeç değişince not silinir.
- **Süzgeç → kural çevirisi tek yerde:** `features/products/filters.ts` `watchQueryFromFilters(search, filters)` (desteklenmeyen alanları atar, hiçbir alan kalmazsa `null`) ve `unsupportedWatchFilterLabels(filters)`. Kural **adı gönderilmez**: sunucu süzgeçten okunur bir ad üretir (`describeWatchQuery`).

## Teklif akışı (Faz 2, Adım 2)

Plan: `docs/faz2-plani.md` Adım 2. Sunucu `backend/src/routes/quotes.ts`; istemci uçları `api/client.ts` "Teklif akışı" bölümünde. **Teklif istemek için bağlantı şart değil** (Fırat'ın kararı 2026-09-17): eski sohbet açan `requestQuote` ve 403 + bağlantı önerisi kaldırıldı. Fiyat yalnızca iki tarafa görünür; alıcı satıcının **taslağını hiç görmez**.

- **Giriş noktaları:** akış kartındaki 44px kare "Teklif iste" (`pricetag-outline`) · ürün sayfasının alt çubuğunda ikincil outline "Teklif iste" (kendi ürününde ikisi de yok). Ürün sayfasında bu düğme eski "Firma" düğmesinin yerini aldı: üç `lg` düğme 375px'e sığmıyor, firmaya gidiş zaten çubuğun üstündeki firma satırında.
- **Form (`QuoteRequestForm: { productId, productCode, stockUnit? }`):** beyaz bloklar — başlıkta mono ürün kodu, Miktar (sayı) + birim (`ChipSelect` m/kg, varsayılan ürünün stok birimi), İstenen termin tarihi (YYYY-AA-GG, sertifika tarih alanı deseni), Not; altta sabit çubukta "Teklif iste". 409 `already_open` → satır içi hata + "Mevcut isteğe git". Başarıda `replace('QuoteRequestDetail')` (geri tuşu doldurulmuş forma dönmesin).
- **Durum rozeti (`QuoteStatusBadge`):** open "Teklif bekleniyor" mavi · quoted "Teklif verildi" turuncu · accepted "Kabul edildi" yeşil · declined "Reddedildi" kırmızı · cancelled "Geri çekildi" gri. `SampleStatusBadge` deseni; metin sunucudan gelmediği için eşleme bileşenin içinde.
- **Detay (`QuoteRequestDetail: { requestId }`)** tek ekran, iki rol; rol **sunucudan** gelir (`request.role`). Üstte özet bloğu: ürün kodu (dokununca ürün sayfası), durum rozeti, karşı taraf (alıcıya satıcı firma, satıcıya alıcı adı + firması), istenen miktar ve termin, not.
  - **Alıcı:** `activeQuote` varsa kesik çizgili **teklif kartı** (PassportCard görsel dili: ince çerçeve + `borderStrong` dashed iç çerçeve): "TEKLİF" başlığı, büyük mono birim fiyat, altında çizgili satırlar (Toplam — yalnızca fiyat birimi istenen birimle aynıysa —, En az sipariş, Termin, Geçerlilik, Ödeme), not. Alt çubuk "Reddet" (outline) / "Kabul et" (primary), ikisi de `confirmAction`. Süresi dolmuş teklifte sarı uyarı + düğmeler kapalı. Teklif yokken "Satıcı teklif hazırlıyor" + "İsteği geri çek".
  - **Satıcı:** üstte outline "Ürün kaydından doldur" (`draftQuote`); `draftInfo` sarı kutuya çevrilir ("Üründe fiyat kayıtlı değil, fiyatı siz girin", "İstenen miktar MOQ'nun altında", "Fiyat kg'dan metreye çevrildi; toplam ..."). Form: birim fiyat + para birimi + fiyat birimi (`ChipSelect compact`), MOQ + birimi, termin (gün), geçerlilik tarihi, ödeme koşulu, not, altında "Fiyatı yalnızca siz ve isteği açan taraf görüyor". Alt çubuk "Kaydet" (outline) + "Teklifi gönder" (primary; **önce kaydeder sonra gönderir**, `price_required` alan hatasına düşer). Daha önce gönderilmiş teklif varsa üstte "Gönderilen son teklif" özeti ve düğme "Revize teklifi gönder". İstek kapandıysa form yerine son teklif + tek satır açıklama.
  - Form yalnızca **istek başına bir kez** dolduruluyor (odak yenilemesi yazılanı silmesin); `draftQuote` sonucu alanları açıkça yeniden yazar.
- **Listeler (`QuoteRequests: { role? }`):** üstte iki eşit sekme düğmesi "Verdiğim istekler" / "Gelen istekler" (firması olmayanda sekme çubuğu hiç çizilmez). İki liste **tek yüklemede** geliyor, sekme değişince istek atılmıyor. Satır: mono ürün kodu + durum rozeti, karşı taraf, miktar + göreli zaman, ok. Girişler: Profil menüsünde "Tekliflerim" · Firmam sayfasında "Gelen Talepler"in altında outline "Gelen teklif istekleri (N)" (N = açık istek sayısı, odakta tazelenir).
- **Bildirimler:** `quote_*` türleri `pricetag-outline` ikonuyla çizilir ve `data.quoteRequestId` ile detaya gider (id yoksa listeye: `quote_request_new` satıcı sekmesine, diğerleri alıcı sekmesine).

## Basma geri bildirimi (4. aşama)

- Beyaz satır/blok basılıyken zemini `colors.pressed` olur; Android'de ayrıca `android_ripple`.
- Dolu lacivert düğme basılıyken `opacity 0.85`; çerçeveli/tonlu düğmenin zemini `pressed` olur.
- Satır içi metin bağlantısı ve ikon düğmesi basılıyken `opacity 0.6`.
- Lacivert bant üzerindeki düğmeler basılıyken hafif açık zemin (`rgba(255,255,255,0.14)`).

## Titreşim sözlüğü (`src/features/haptics.ts`)

| Çağrı | Ne zaman |
|---|---|
| `haptics.selection()` | Seçim değişti (çip, birim, sekme) |
| `haptics.light()` | Hafif anında onay (beğen) |
| `haptics.success()` | **Önemli** bir işlem sunucuda tamamlandı: kaydet, talep adımı, sil, bağlantı kur/kabul |
| `haptics.warning()` | Geri alınamaz işlemin onayı soruldu (`confirmAction` destructive'de kendisi çağırır) |
| `haptics.error()` | İşlem başarısız |

Basmanın kendisi titreşmez. Sohbet mesajı ve yorum gibi **sık** işlemlerde başarı titreşimi yok, yalnızca hata. Web'de titreşim kapalı.

## Web kuralları

- **Yığında geri dönüş `popTo` ile.** React Navigation 7'de `navigate` var olan ekrana geri gitmez, ikinci kopyasını açar (2026-09-16'da onay ekranı → form ve ürün seç → gönderi akışlarında görüldü). Bir alt ekrandan üst ekrana sonuç taşımak için `navigation.popTo('Ekran', params, { merge: true })`.
- **İç içe düğme yok.** İçinde başka dokunulabilir öğe olan satırda web'de rol verilmez: `accessibilityRole={Platform.OS === 'web' ? undefined : 'button'}` (react-native-web "button" rolünü gerçek `<button>` yapıyor).
- **`Alert.alert` kullanılmaz.** react-native-web'de boş bir fonksiyon (hiçbir şey göstermez). Onay için `src/features/confirm.ts` → `confirmAction()` (web'de tarayıcı onayı, telefonda sistem penceresi).
- `expo-secure-store` web'de yok; oturum jetonu `src/features/tokenStorage.ts` üzerinden.

## Erişilebilirlik

- İkon düğmelerinde `accessibilityLabel` zorunlu (gizli etiketli `HeaderButton`, sayaçlı eylemler: "Beğen, 24").
- Satırlarda okunabilir birleşik etiket ("ORM-0587, Bursa Örme San., Hazırlandı. Takibi aç").
- Metin karakteri ikon olarak kullanılmaz (✓ ✕ ♥ →); Ionicons.
- Görünür metinde uzun tire (—) kullanılmaz.
- İskelet "hareketi azalt" açıksa sabit durur.

## Yeni düzene henüz geçmeyen ekranlar (2026-09-15)

Taslaklarda karşılığı olmayanlar; aynı kalıplarla uyarlanmalı:

- Kayıt adımları: `RoleSelection`, `Position`, `PersonalInfo`, `CompanyInfo`, `PhoneVerification`, `CompanyCode`, `OnboardingLayout`, `OtpCodeField`
- Formlar (`AddProduct` 2026-09-15'te geçti): `CreatePost`, `EditCompany`, `SampleRequestForm`, `TextField`, `ChipSelect`
- 7 hesaplama formu, `GarmentVisualCost`, `Admin` (`Advisor` kaldırıldı, yerine Asistan geldi)
- `ImageViewerModal` (✕ karakteri), kalan uzun tireler: `FabricWeightCalculator`, `CompanyCodeScreen`, `GarmentVisualCostScreen`
